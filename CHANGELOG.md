# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Version numbers follow [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added
- **Landing page (2026-09-08, Phase 8 GTM / UD-01):** built the first landing surface per `docs/specs/FEATURE_SPEC_landing_page.md` — `landing/index.html` (self-contained, responsive, honest). Hero uses the precise, provable claim **"The AI test generator where your data never leaves your deployment"** (not "never touches your data" — which is false in the RAG/reference-docs case; the sub-line acknowledges it learns from your reference docs and they stay in-house). Real product screenshot (a captured `launch_ui.sh` Streamlit run, not an AI mock) + a toggleable noir-artwork view. How-It-Works, an interactive DOM-resolution pipeline demo, the regulated-defense bento (BYO-LLM, **bounded egress verified in CI** via `python scripts/audit_egress.py`, self-healing, role-based evidence = real heatmap for product owner / Gantt timeline for test manager / JUnit/HTML/CSV, CI/CD + POM), the no-egress trust section linking the **published egress audit** + `SECURITY.md`, and per-deployment pricing tiers (Free / Pro / Air-gap) each citing a **real comparable anchor** (Mabl 500-credit, testRigor $450/mo + Mabl $499/mo, QA Wolf $8k/mo + $90k median ACV) with a footnote to `RESEARCH_COMPETITIVE_LANDSCAPE.md §4.2`. TanCat logo wired into nav + footer. No fabricated metrics, no `0.00 KB egress`, no `iptables`/`eBPF` enforcement over-claims, no invented star counts, `© TanCat` (no Ltd — not yet incorporated), `tancat-0.1.0`. Honesty pass: every claim cross-checked against the code (`src/licensing/` per-deployment license confirmed real; heatmap/Gantt artifacts confirmed shipped + validated by `artifact_validation.py`). Verified: renders desktop (1280) + mobile (390) with zero JS errors and no horizontal overflow.

### Changed
- **Evidence screenshots are now WebP, encoded losslessly with Pillow (2026-09-11):** evidence output was PNG; it is now WebP by default, cutting the footprint roughly in half at *identical* fidelity. Verified live against the same suite and the same site (17 screenshots): **PNG 2501 KB -> WebP 1279 KB** (51%, i.e. 49% smaller). Pixel-identity is guaranteed by encoding losslessly (`lossless=True, quality=100`) — evidence is an audit artifact, so the lossy path (q90 ≈ 45%, q80 ≈ 33%) is deliberately not exposed as an option.
  - New module `src/evidence_image.py` (`encode_evidence_image`, `write_evidence_image`). Format selection lives in `src/config.py` via `evidence_image_format()` / `evidence_image_extension()` (default `webp`), overridable with `AITEST_EVIDENCE_IMAGE_FORMAT=png` as an escape hatch for a consumer that cannot read WebP.
  - **Do NOT capture with Playwright's `type="webp"`:** Chromium's WebP *lossless* encoder produced files **4.2x larger** than its own PNG output for the real saucedemo login page (111.8 KB vs 26.5 KB at 1280x720). The capture path therefore takes a PNG screenshot and re-encodes it with Pillow. This was only visible by measuring real pages — a naive `type="webp"` switch *appears* correct (it really is lossless) while inflating evidence 4x.
  - Wired into `src/evidence_tracker.py` (runtime per-step evidence) and `src/cli/evidence_generator.py` (CLI capture). `scripts/verify_production.py` now counts both `.webp` and `.png`; the three report/heatmap MIME maps already handled `.webp`, and every evidence consumer globs `*.evidence.json` rather than image extensions, so no reader changes were needed.
  - +19 tests in `tests/test_evidence_image_format.py` pinning the WebP default, the PNG escape hatch, lossless pixel-identity, size reduction, and the bad-input fallback (evidence capture must never fail on an image problem). Gates: **3123 passed**, smoke 39/39, eval static 97.9% / 79.0%, ruff clean, mypy `src/ cli/` clean.
- **Dependency refresh, each upgrade individually gated (2026-09-11):** upgraded 17 dependencies, every one validated through the same gate set (ruff → `mypy src/ cli/` → full `pytest tests/` → `smoke.py` → eval-harness static) before keeping it. **Safe batch:** `ruff` 0.16.0→0.16.7, `mypy` 2.3.0→2.3.1, `playwright` 1.61.0→1.62.0, `pytest-playwright` 0.8.0→0.9.0, `langgraph` 1.2.10→1.2.11, plus `python-dotenv` 1.2.2→1.2.3, `pydantic` 2.13.4→2.13.5, `numpy` 2.5.1→2.5.3, `packaging` 26.2→26.3, `typer` 0.27.0→0.27.2. **Majors, one at a time:** `cryptography` 49.0.0→50.0.1, `plotly` 6.9.0→7.0.0, `streamlit` 1.60.0→1.63.0, `transformers` 5.14.1→5.17.0, `torch` 2.13.0→2.14.0, `sentence-transformers` 5.6.1→6.0.1, `openai` 2.50.0→3.13.0. **Result: no regressions** — every run held at 3104 passed / smoke 39/39 / eval static **97.9% resolution, 79.0% skeleton** (resolution accuracy never moved, confirming the resolver is untouched). `.pre-commit-config.yaml` revs realigned to the new tool versions (ruff `v0.16.7`, mirrors-mypy `v2.3.1`) so pre-commit and CI run the same linters. Notes: `playwright` 1.62 needs `playwright install chromium`; `sentence-transformers` 6.x keeps the embedder identity (`all-MiniLM-L6-v2@384`, still 384-dim) so existing RAG stores stay valid; `openai` 3.x added `httpx2`/`httpcore2`/`truststore` — and `openai` has **no imports anywhere in the repo**, so that major bump was inert.
- **PDF ingestion outcome semantics clarified (2026-09-07):** `src/pdf_ingest.py` now distinguishes `"empty"` (page was checked via OCR but no usable content found — reasons: `ocr_no_text`, `ocr_failed`) from `"skipped"` (page was NOT checked at all — reason: `no_engine`, the `[ocr]` extra is not installed). Previously both cases produced `"skipped"`, making the CI regression test (`test_lv_docs_no_pages_skipped_regression`) unable to tell a genuinely blank page from a lost content page. The test now correctly asserts 0 `"skipped"` (0 pages not checked) while allowing `"empty"` pages. Also added **FC-06 — VLM-Powered Table OCR** to the roadmap (future item, not required for launch): uses the customer's already-loaded LM Studio model as a vision-language model for scanned/image-only PDF pages — zero new dependencies, works air-gapped, no PyTorch, no model download. Research compared Docling, PaddleOCR PP-StructureV3, LlamaParse, pdfplumber, Tabula, and the VLM approach. Installed `PyMuPDF` (the `[pdf]` optional extra) which was previously off — all OCR backend tests now pass. Gates: 3104 passed / 0 failed, smoke 39/39, ruff + mypy clean.
- **Brand rename to TanCat (2026-09-06, AI-039):** the product is now **TanCat** across the distribution + customer-facing surface. Distribution name `playwright-test-generator` → `tancat` (`pyproject.toml`; `uv.lock` regenerated via `uv lock`). Repo/display name → `tancat-ai/tancat` (README, AGENTS, CONTEXT, CONTRIBUTING, SECURITY, Dockerfile, docker-compose, ARCHITECTURE, ci.md, walkthrough, kanban.html, markdown_docs, mock_sites README, scripts, `.env.example`, `src/` docstrings). **Customer-facing banners** (the part the exact-string pass missed): Streamlit `<h1>`/`st.title`/browser-tab title → **TanCat**; CLI banner `print_header` + `menu_renderer` header comparison + subtitle → **TanCat**; HTML report footer → "Report generated by TanCat". Historical docs (`docs/sessions`, `docs/archived`, `docs/specs`, …) intentionally left as records of the former name. No import-path change was needed — code imports `src`/`cli`, not `playwright_test_generator`. Gates: smoke 39/39 (egress audit 0 flagged), ruff clean, **3101 passed / 0 real failures** (2 transient empty-subprocess flakes in `test_cli_e2e.py` — pass in isolation; `cli/main.py --help` verified working + shows TanCat). Companies House incorporation **deferred** (no funds this month; income/projections first) — landing page uses "© TanCat" (not "© Cat Tan Operations Ltd") until incorporated. GitHub org `tancat-ai` + repo `tancat-ai/tancat` is a git-remote action (not in this diff).

### Fixed
- **Stray `nul` file was crashing the type gate (2026-09-11):** a 40-byte gitignored `nul` artifact (dated 2026-08-23) in the repo root made `mypy .` fail outright with `ValueError: path is on mount '\\\\.\\nul', start on mount 'C:'` — mypy never completed, so the type check had effectively not been running. Removed the file; both `mypy src/ cli/` (CI scope, 156 files clean) and `mypy .` now execute.
- **Streamlit 1.63 `AppTest.from_file()` path resolution (2026-09-11):** Streamlit 1.63 resolves a relative `script_path` against the **file that calls `from_file()`**, not the current working directory, which broke 51 tests across 4 files (`FileNotFoundError: tests/streamlit_app.py`). The 4 call sites now compute an absolute path at module level (`APP_PATH = str(Path(__file__).resolve().parents[1] / "streamlit_app.py")`) — deliberately outside the `Path.exists` patches those fixtures install. All 51 then passed, confirming 1.63 compatibility rather than merely silencing the error.
- **B-050 — License trust root was overridable via `AITEST_LICENSE_PUBKEY` (2026-09-06)**: `src/licensing/license.py` read the `AITEST_LICENSE_PUBKEY` env var as the trust root, letting any **stock** build self-sign its way into paid tier **without a fork** — the exact no-code-change bypass the open-core posture ("a fork is the accepted bypass") was supposed to require. **On-book fix (option 1):** removed the override entirely — the trust root is now always the vendored key, rotation ships with a product release (the standard offline-licensing rule: *if offline, vend the key with no override and rotate via release*; the customer must not be able to set the trust root). `vendor_public_key()` returns `VENDORED_PUBLIC_KEY_B64`; `verify_license()` uses the constant; the 3 end-to-end tests that pointed the deployment at a test key now monkeypatch the vendored constant (still exercising the real load/verify path). Verified live: an attacker self-signed `pro` token is now `invalid`, and setting `AITEST_LICENSE_PUBKEY` is ignored. Docs updated: `docs/security/license-key-ops.md` (§1/§7), `docs/specs/FEATURE_SPEC_phase6_saas.md` §5.4, `markdown_docs/src/licensing/license.py.md`. The env var was originally built for *per-customer signing keypairs* — if that need is real later, the on-book path is server-issued offline-verified (a separate item), not resurrecting the env var. Suite 3104/0, ruff + mypy clean, smoke 39/39 (egress 0 flagged).
- **B-048 — RAG store seeding gap (2026-09-06)**: the AI-059 lab's warm-store rebuild (`restore_store_snapshot`) could wipe the PRODUCTION RAG store while the idempotent seed marker survived, leaving `ensure_bundled_seeded()` to skip re-seeding forever (this caused the 2026-08-31 → 09-05 measurement blackout: all full evals ran with golden bonus = 0). Both backlog guards shipped: **(a)** `restore_store_snapshot` refuses any target that is / contains / lies inside the production `rag_path()` or its `.embedder.json` companion unless `allow_production_store=True` is passed; **(b)** the seed marker now means "seeded AND golden count > 0" — a stale marker on a pattern-less store re-seeds the bundled pack and reports the new `"reseeded"` status (also fixes the latent no-marker docs-only-store variant that previously marked itself and stayed golden-less). 7 new tests; full suite 3104/0; real store verified `{golden: 113, doc: 101}` with steady-state skip; eval static green. markdown_docs for both modules updated.

### Added
- **Security + commercial audit follow-ups (2026-09-06)**: full-repo audit (product/commercial, security, bloat) actioned. **New backlog items:** B-050 (license trust root overridable via `AITEST_LICENSE_PUBKEY` on stock builds — policy decision needed: remove / config-file / document), B-051 (free-tier metering trivially resettable — honesty note + optional tamper-evident ledger later), B-052 (❓ needs-info — prompt-injection exposure of skeleton/resolver prompts; research-first, no action), B-053 ✅ (license key-ops documented → new `docs/security/license-key-ops.md`: generation, two-location backup + tested recovery, rotation-as-release, revocation-by-expiry, single-operator risk). **B-048 flagged 🚩 PRE-LAUNCH BLOCKER.** Roadmap: README landing-page demo fix folded into UD-01; Phase 8 gained 4 pre-launch items (payment → license fulfillment, launch-readiness un-deferral sweep, business plan + pricing validation research, GTM strategy doc refresh gated on GTM readiness). Records corrected: `RESEARCH_SAAS_AND_LAUNCH.md` §8.4 screenshot-credential-redaction row → RESOLVED (verified shipped 2026-08-25, `src/credential_redaction.py` + `EvidenceTracker:346`); AE scraper-timeout hypothesis CLOSED — `verify_production automationexercise` 541s pipeline with no hang (B-049 not opened); the AE FAIL 11/13 verdict is the known ASSERT-resolution class (2 unresolved ASSERT placeholders → honest skips; 5 passed / 2 skipped live). Housekeeping: stale mypy `map_3d.generate_3d_map` override removed from `pyproject.toml` (mypy re-verified clean).
- **Pipeline-direction consolidation (2026-09-05)**: merged the BACKLOG LangGraph experiment, AI-054 §1, and Phase 1's dormant note into ROADMAP §12d (Hybrid — LangGraph-orchestrated linear pipeline, graph as orchestrator over the linear core), with updated data: the historical 88.1%-vs-32.8% gap was inflated by the fixed graph mock-ensure bug (clean: linear 54.9% vs graph 45.1%); demoqa story-bleed root-caused; graph wins the multi-step LV form. BACKLOG reduced to one-line pointers per AGENTS.md §10.
- **Phase 6 Part 1 shipped — 6c/6d/6e/6h/6i (2026-09-05)**: the commercialisable v1 (per-deployment, BYO-LLM) is now code-complete. **6c team concurrency** — `src/rag_store_lock.py`, a stdlib cross-process advisory write lock (msvcrt/fcntl) keyed on the store path, re-entrant within a process, wired into the three `MilvusLiteBackend` write chokepoints so concurrent teams/CI can't corrupt the single-writer RAG store (2 real `multiprocessing` race tests). **6d BYO-LLM health check** — `src/llm_health.py` (`check_llm`, `HealthCheckResult`, `render_report`): reachability → key → model-in-list → capability probe (thinking-off, temp-0), reusing the product's own `LLMClient` construction path; 🩺 Check My LLM in the Streamlit sidebar + a CLI menu item; `RECOMMENDED_MODELS` table. **6e offline license + tiers + free tier** — `src/licensing/` (`tiers.py` data-driven free/self-serve/pro/airgap table; `license.py` hand-rolled ed25519 sign/verify, 7-day grace, `unlicensed = free`, `feature_enabled`; vendored pubkey with `AITEST_LICENSE_PUBKEY` override) + vendor tool `scripts/license_gen.py` (gen-keys/sign); `src/usage_meter.py` (30-day runs/exports window on `run_results.sqlite` + local export ledger, free-tier caps `AITEST_FREE_TIER_RUNS=25`/`_EXPORTS=10`/`ENFORCE_FREE_TIER`), run gate in `PipelineRunService`, export recording in `evidence_export`, license+usage sections in `ci_generate --json` + `AITEST_ENFORCE_LICENSING` opt-in gate, sidebar Usage panel + license banner. Zero new outbound HTTP (egress audit still 0 call sites flagged). **6h latency benchmark + LLM-call cache** — `src/llm_cache.py` (disk TTL cache keyed on provider+model+temp+thinking+prompt, atomic writes, `AITEST_LLM_CACHE=0` opt-out; `CachingGenerator` wired into the semantic ranker's expensive resolution calls, only for real `LLMClient` generators) + `scripts/benchmark_latency.py` (per-model-tier table vs the 180s/6-criteria SLO, `--self-test` hermetic, `--save` published JSON). **6i multi-site eval re-validation** — `scripts/eval/revalidate_goldens.py` (live golden-key re-validation: real chromium for live sites, `MockServer` for mocks, OR tolerance semantics, stateful-page classification so login-gated/cart-seeded/post-submit goldens are recorded honestly, recency record `scripts/eval/revalidation/latest.json`); golden refreshes (automationexercise Products/Cart-link tolerances, the-internet `h1.heading`, ambiguous Place-Order expected_page fix); `baseline.json` regenerated to the multi-site truth (9 datasets · 7 sites · 96 placeholders · 97.9% static). Live re-validation 2026-09-04: 8/8 evaluable datasets pass, LV honest static-only. Gates: 3088→3095 pytest, ruff + mypy clean, smoke 39/39 incl. egress audit 0 flagged.
- **Eval investigation — RAG store wipe root-caused + eval-harness graph mock-parity fix (2026-09-05)**: the full-eval regeneration-accuracy drop (53.2–54.1% reference band → 42.5–48.7%) was root-caused to a **wiped RAG store**: the AI-059 lab's warm-store rebuild does `shutil.rmtree` on the production store dir while the idempotent seed marker survives, so `ensure_bundled_seeded()` never re-seeds (golden pack = 0 in every eval since 2026-08-31). Repaired with `rag_ingest.py --bundled --force` (113 golden + docs); guard tracked as **B-048**. Also fixed an eval-harness bug: `_regenerate_code_via_graph` never calls `_ensure_mock_serves` (the linear path does) → graph-mode mock datasets scraped a dead `:8781`; parity fix + source-contract regression test. Controlled build comparison (identical settings, restored store): **mainline 54.9% vs fork 48.7% (−6.2pp)** — the fork llama.cpp build costs resolution quality; mainline stays the generation engine. Clean graph-vs-linear: **54.9% vs 45.1% (−9.8pp)** with graph's demoqa collapse root-caused to skeleton story-bleed. All recorded in `docs/sessions/2026-09-04_phase6_6c_6d_6e_6h_6i_handoff.md`.
- **Doc discipline — AGENTS.md §10 split-of-responsibility (2026-09-05)**: roadmap owns big features/phases; `BACKLOG.md` owns fixes/smaller changes; one canonical location per item with one-line pointers elsewhere; verify shipped-ness against code, not docs (the false "6b–6i shipped" spec line that triggered this).
- **16b Test-to-Document Traceability (Cited Generation) — all 4 phases shipped (2026-09-02)**: implemented the full cited-generation feature per the 2026-09-01 spec (D1–D12) so a generated figure can answer *"where did you get that number from?"* — or honestly say **⚠ no source found**. **Phase 1 — data model:** `src/source_refs.py` `SourceRef` (doc, page_pdf, page_label, heading, quote, route, dedup_key, kind=cited/unresolved) + `verify_quote()` (normalized substring, no fuzzy — a proven quote or an honest ⚠, never a confident guess); provenance fields on `DocChunk` (page/page_label/route) and `RetrievedPattern` (doc_source/doc_page/doc_page_label/doc_route) carried through `add_docs()`/`retrieve()`; `Criterion.source_refs`+`.justification` with `director.py` pass-through (D12). **Phase 2 — whole-doc generation:** `src/pdf_ingest.py::ingest_pdf_page_aware()` chunks per page (the page number no longer dies in whole-doc pulping) with per-page OCR fallback; `pipeline_graph._parse_document` (protected, sign-off via spec) feeds the FULL document into `user_story`, removing the `[:500]` ceiling so a page-30 boundary figure reaches the LLM. **Phase 3 — citations:** `src/citation_verifier.py` — `attach_paste_citations()` (deterministic: the criterion IS the line) + `verify_document_citations()` (hybrid: LLM proposes quote+page, code verifies → proven / corrected-page / unresolved ⚠; justification only when verified, capped 400; dedup_key pinned on every proven ref). **Phase 4 — surfaces:** `src/citation_surfaces.py` — `render_source_comments()` (test-file `# Source:` blocks, the artifact users keep), `render_citation_cards()` (Living Test Plan), `render_cli_debug()` (per-criterion dump + trust-boundary footer), `render_export_note()`; `PRIVACY_MODE` = pointer-only (quotes omitted). ⚠ never hidden (D9). 69 new tests (`test_source_refs` 32, `test_16b_phase2` 6, `test_16b_phase3` 15, `test_16b_phase4` 16). Gates: smoke PASS, ruff check + format clean, mypy clean (src/ cli/ + broader pre-commit on test files), **3029 pytest, 0 regressions**.
- **16b Test-to-Document Traceability (Cited Generation) — full spec (2026-09-01, docs-only)**: spec session complete; decisions D1–D12 locked in `docs/specs/FEATURE_SPEC_test_to_document_traceability.md` (canonical; supersedes the 2026-08-25 `SESSION_SPEC` notes). Roadmap §16b slimmed to the phase tracker. Scope: citations per criterion (LLM proposes verbatim quotes, code verifies), whole-document generation (Phase 2 merged with AI-055's remaining per-page OCR wiring), bounded quotes + umbrella `PRIVACY_MODE`, advisory ⚠ unresolved with per-figure precision, test-file `# Source:` comments / plan citation cards / CLI debug surfaces. Tier 3 (figures/tables-as-image) documented as a limitation; future figure/table structure extraction + figure-region preview owned by §16. BACKLOG: AI-065 watch item (citation/rationale token overhead) opened.
- **AI-055 — Ingestion Improvements (Local): tiered CPU-first OCR + format scope + quality summary + cause-differentiated skip warning (2026-08-25)**: the *product* layer on top of the AI-045 #4 PDF OCR wiring — make ingestion work on *any* customer hardware (CPU-first, no GPU), promise a defined format scope, and tell the customer what happened. **(1) Tier-1 CPU OCR backend**: `RapidOCRBackend` + `AutoOcrBackend` in `src/ocr_backends.py` — the `auto` (default) backend composes tier-0 PyMuPDF (whole-doc, zero OCR cost) + tier-1 RapidOCR/PP-OCR (per-page, image-only fallback) via ONNX Runtime = pure CPU, ~50–80 MB, no network, no GPU. `get_ocr_backend()` selection extended: `auto` / `cpu` / `high-accuracy` (tier-2, not built in v1 → falls to CPU) / `power` (tier-3 GPU VLM, falls to CPU if no GPU); legacy `pymupdf`/`unlimited-ocr` names still map correctly. New optional `[ocr]` extra (default install stays light) — fixed a `oocr`→`ocr` typo in `pyproject.toml` that would have broken `uv sync --extra ocr`. **(2) Ingestion quality summary** in `src/rag_bundled.py` + `scripts/rag_ingest.py`: per-doc outcome (full/partial/skipped), page text/OCR/skip counts, dedup new-vs-present, actionable suggestion. **(3) Cause-differentiated skip warning** (the trust signal): a skipped page surfaces as `[WARN] <doc>: N page(s) (cause) -> NOT digested` and **does not hide behind an overall green result** — causes: `no_engine` (engine not installed → shows the exact install fix `uv sync --extra ocr` + docs link), `ocr_no_text` (OCR ran but couldn't read — no install fix shown), `ocr_failed` (OCR hook raised). Serves the use case: a scanned doc in a bigger pack that gets a positive result is still flagged as **not digested** + the one-line fix. **(4) Format scope**: pdf + md in; unknown formats rejected loudly (`.txt` deliberately not in v1). **(5) CI regression test** (`test_lv_docs_no_pages_skipped_regression`) — the durable form of the ingestion tracking; LV docs must ingest with 0 pages skipped, or CI goes red before the regression ships. **(6) Dedup-key source-in-hash guarantee** tests (two different docs with identical text never dedup against each other) + stale-lingering-on-append behavior documented. **End-to-end verified on the LV car-insurance docs** (3 PDFs, all text, zero full-page scans → 66 chunks, all "fully ingested"). Known ceiling (documented, not a bug): page-OCR handles *text* in images, **not** tables/graphs rendered as images. Remaining (follow-up, protected-file sign-off for the first): wire per-page OCR into the generation pipeline's direct-doc parse; optional most-recent sidecar manifest. Gates: 2960 pytest, smoke 39/39, ruff check + format clean, mypy clean (src/ cli/ + staged), eval static 97.9% (no regression).
- **AI-058 metric gate — controlled ambiguous mock + DETERMINISTIC resolver-level proof (2026-08-30)**: the available mocks never emit a *recoverable* wrong-element failure (clean mock resolves correctly → no signal; hard mock times out on a single candidate → a negative can't steer), so the `mean_pass_depth` gate had nothing to measure against. Built a purpose-built **ambiguous mock** (`mock_sites/ambiguous/`, dataset `eval-010`) whose confirmation page offers 2+ genuine candidates for the "order success message" step: the golden `#order-success-message` (112), the other correct `#order-success-title` (95), and a VISIBLE text-overlap **TRAP** `#order-note` (32, shares the "Your order ..." prefix) — the banking `#payment-error` trap, kept visible so the scraper actually offers it as a candidate. Two tools: `scripts/ai058_ambiguous_seeded_ab.py` (the generation-level seeded A/B — control vs a step-scoped negative on the trap; not runnable on this box today because the Qwen3.8-27B skeleton generator burns the token budget producing degenerate output, an LLM/model-state issue) and `scripts/ai058_ambiguous_resolver_ab.py` (the **deterministic resolver-level** A/B — frozen `scraped_pages/` pool, no LLM, no mock server). **All 6 resolver checks pass**: the trap is a real candidate in the control pool; control ranks the correct winner; **the step-scoped negative demotes the trap (32 → 12)**; treatment keeps the correct winner; the correct winner's score is unchanged (112 = 112); a different step ('Back to Store') is byte-identical with/without the negative (no cross-step bleed). The same deterministic proof pattern that shipped under AI-064, now on a mock built specifically to expose the flip — the AI-058 gate is demonstrable at the resolver level. `eval-010` contributes 0 placeholders to the static gate (no frozen scraped data / golden run), so the baseline is unchanged. Gates: 2907 pytest, smoke 39/39, ruff + mypy clean (scripts/ not gated), eval static 97.9% (94/96, no regression).
- **AI-064 resolver-level A/B — deterministic flip proof (2026-08-29)**: `scripts/ai058_resolver_ab.py` + unit test `test_resolver_ab_downweights_wrong_pick_on_own_step`. Uses ONLY the frozen `scraped_pages/` dumps (no LLM, no mock server, no scrape variance). Three legs on the same frozen pool with the sole variable being one seeded negative on `#payment-error` for ASSERT 'payment success message': (1) payments-only context (the historical test_09 wrong-pick case) — control ranks `#payment-error` first (52), the seed drops it to 32; (2) consolidated pool — the correct `#payment-success-message` (112) already wins post-AI-064 and the negative cannot hurt it; (3) step-scoping guard — a different step ('payee') is unchanged. Proves the step-scoped negative down-weights the wrong pick on its own step and never leaks, with zero run-to-run variance (the LLM-variance problem of the generation A/B is removed entirely). Gates: 2907 pytest (+1), ruff + mypy clean, eval static 97.9%.
- **AI-064 seeded-store A/B + isolation audit (2026-08-29)**: new hermetic `scripts/ai058_seeded_ab.py` — a temp-dir driver (own `AITEST_STORAGE_ROOT`, auto-learn off, read-only mock server) that seeds ONE known banking negative (`#payment-error` for ASSERT 'payment success message') and proves the step-scoped flip: insert → `find_negative` row → score −4 on `#payment-error` / 0 on `#payment-success-title`. Leak-audited: no mock modification (restored via git), no `baseline.json`/`eval_results.db`/`generated_tests/` writes — all runtime artifacts stay in the temp dir. Deterministic generation-level A/B (control vs seeded) produced identical locators this run (resolver pick is LLM-variance dominated), so the scoring-level seed test is the deterministic proof. 1 new test (`test_seeded_negative_roundtrips_and_flips_step_scoped_score`). Gates: 2906 pytest, eval static 97.9% (no regression).
- **AI-064 — container-element haystack dominance fix (2026-08-29)**: the `main`/`body`/`div`/`section`/`nav`/`form` container's haystack is the MERGED text of every descendant, so it matched ANY description appearing on the page and tied the specific element at 100 — then won the tie-break on text length. This made banking's `main:has-text(...)` failures look "unrecoverable" (the resolver only "saw" the container) and masked real candidates like `#success-title`. `PlaceholderScorer._container_aggregate_penalty` now applies −40 on the haystack fast path: (a) generic containers for all actions EXCEPT page-level ASSERTs (kept sanctioned path); (b) for CLICK, non-interactive prose text-runs (`p`/`h1-6`/`li` with no link/button role/href) are also penalized so a real interactive target always wins — this fixed the follow-on where `.text-center` won "Transfer Money" after `main` was penalized. Verified live: "payment success message" resolves to `#success-title` (was `main`); banking "Transfer Money" → `#transfer-link` (was `main:has-text(...)`/`.text-center`). Banking A/B cold mean_pass_depth 0.900 → **0.960**, CLICK `main:has-text` failures eliminated. Eval static 97.9% (no regression), 2905 pytest (+5), ruff + mypy clean. Still open: the metric gate's remaining ASSERT failure is a mock-fit issue (balances rendered via JS into `main`, no specific element) — not the container dominance.
- **AI-063 — step-scoped learned negatives + resolved-but-wrong trigger (2026-08-29)**: the AI-058 negative mechanism is now safe to apply and learns from the real production pain (a "high-scoring locator that keeps failing").  (1) **Step-scoped matcher**: `PlaceholderScorer._learned_net_evidence` / `_learned_negative_penalty` now gate on `(action, description)` via `_step_scope_matches` (strips the stored `ACTION: ` prefix, case-insensitive) — a negative only applies on the step it was recorded on, for BOTH positives and negatives, closing the "Cart link" cross-step leak (same locator fine on step A, known-bad on step B). (2) **Resolved-but-wrong trigger**: a failed `ASSERTION` step with a resolved selector is now a `learned_negative` at conf 0.6 (locator-timeouts stay 0.9) — the element existed, was picked, and failed its check — mirrored in `src/rag_learn.py` and the `learning_impact.py` lab rebuild. (3) **Hidden classifier gap fixed**: `Locator.wait_for: Timeout 5000ms exceeded` (EvidenceTracker sync API — no `TimeoutError:` prefix) was classifying as `other`, making the real resolved-but-wrong failures invisible to BOTH gates; `src/failure_classifier.py` now recognizes it as `LOCATOR_TIMEOUT`. **Verified live on real failure data**: rebuild of real failed sidecars → **9 negatives** (was 3), including `#payment-error` (dist 1.000 for "payment success message"), `#empty_cart`, `.login_logo`, `.text`; step-scoped scoring shows `#payment-error` −4 on ITS step and 0 on other steps; the `.add-to-cart` trap does not leak across steps. 14 new tests. Gates: 2900 pytest, ruff + mypy clean, eval static 97.9% (no regression).
- **A/B harness bug fixed — `scripts/ai058_ab_mock_run.py` (2026-08-29)**: the driver's custom conftest wrote `tracker.write(status="passed")` unconditionally, so every prior A/B was blind — sidecars never said `failed`, the negative sweep had nothing to scan, and `green_rate` was faked at 1.000. The template now includes `pytest_runtest_makereport` + the production conftest's `rep_call` status logic. Verified: banking cold went from fake `green_rate=1.000 passed=8` to real `green_rate=0.250 passed=2`, and the warm+neg stores now record real negatives (3 banking, 1 trap-mock). Final A/B re-runs (banking + custom trap mock) show all legs identical (`0.900`/`0.920`) because the mocks+resolver cannot produce a *recoverable* wrong-element pick: single-candidate failures (`main:has-text` is the only candidate), `main`-haystack dominance (aggregated page text always scores 100), and page-context/trail assignment — all resolver-infrastructure blockers (AI-052/AI-054), NOT the negative store. The AI-058 metric gate stays OPEN with the harness now correct and the mechanism fully proven.
- **AI-058 Slice 2 — wire negatives into the sweep + negative-aware warm-store rebuild (2026-08-28)**: the contrastive store (Slice 1) is now fed end-to-end and is A/B-runnable. `src/rag_learn.learn_from_evidence_sidecars` now ALSO scans failed/partial evidence sidecars and records `learned_negative` patterns via `learn_negatives_from_evidence` (locator-class gate unchanged, so infra flakes never poison the store; the passed-only positive path is byte-for-byte unchanged; `learn_negatives=False` restores positives-only). `src/learning_impact.rebuild_warm_store_from_evidence` is now negative-aware — failed/partial `LOCATOR_TIMEOUT` steps are written as `learned_negative` tagged with the same lab sentinel (`hit_count`/`last_seen` intact), so the resolver down-weights them when scoped via `AI059_LAB_SITE_HASH`; `learn_negatives=False` builds the positives-only control store. `scripts/eval/learning_impact.py::rebuild-warm` gained `--no-negatives`; the `baseline` command already supports the `warm-positive` vs `warm-positive-negative` legs, which is the metric-first gate (`warm+negatives > warm` on `mean_pass_depth`). 5 new tests (3 `test_learning_impact` + 2 `test_rag_learn`). Gates: 2891 passed (+5), smoke 39/39, eval static 97.9% (no regression), ruff + mypy clean. **Outstanding:** the live mock A/B (needs mocks + LLM) is the acceptance measurement and was NOT run this session.
- **AI-059 effect trace — decisive counterfactual (2026-08-27)**: extends the Deliverable-2 RAG usage diagnostic to answer whether a retrieved/learned pattern actually *changed the outcome*, not just whether it was applied. `PlaceholderOrchestrator` now re-resolves each placeholder with RAG bonuses stripped (opt-in via `AI059_RAG_DIAGNOSTICS_PATH`) and records `decisive` (True when the no-RAG counterfactual would pick a different winner) and `counterfactual_selector`. Empirical check confirmed it flips correctly (slow-path scenario: a learned +5 tips a 12/12 tie to 17/12). **Root-cause finding:** `PlaceholderScorer.compute_element_score` adds the golden/learned bonus only on the slow semantic path — when the description substring-matches an element (the common haystack fast path) it returns early and the bonus is silently never applied, so `bonus>0` with `decisive:False` is the expected signature of an inert pattern. 1 new unit test (`tests/test_learning_impact.py::test_rag_usage_diagnostic_records_decisive_and_counterfactual`). Gates: 2875 passed, smoke 39/39, eval static 97.9% (no regression), ruff + mypy clean.
- **AI-058 Slice 1 — contrastive learned store: `learned_negative` entries + net scoring (2026-08-28)**: the learned store can now record CONFIRMED WRONG locators and down-weight them at scoring — mirroring how a person learns from mistakes. New: `RAGStore.upsert_negative_pattern` (`entry_type="learned_negative"`, dedup on `(action, description, site_hash)`, `hit_count` + `last_seen` recency), backend `find_negative`, `hit_count`/`last_seen` on `RetrievedPattern`; `rag_learn.learn_negatives_from_evidence` (LOCATOR-class failures only — `classify_failure` → `LOCATOR_TIMEOUT` covers timeouts AND "locator not found"; assertion/navigation/unknown and selector-less steps excluded so infra flakes never poison the store); `learn_from_patch` now also records the OLD selector as a negative (self-heal pairs = the highest-precision contrastive signal); `PlaceholderScorer._learned_net_evidence` resolves a `(desc, selector, site)` pair to ONE net signal — positives − negatives, majority by `hit_count`, recency (`last_seen`) breaks ties with a conservative bias, penalties capped (5×8=40, below +80 structural) — applied on both slow and fast scoring paths; RAG-off mode unchanged. 10 new tests. Gates: 2886 passed, smoke 39/39, eval static 97.9% (unchanged), ruff + mypy clean.
- **AI-062 — RAG bonus applied on the fast path (2026-08-28)**: root-cause fix for the measured ~0% decisive-rate. `PlaceholderScorer.compute_element_score` added the golden/learned bonus only on the slow semantic path; the haystack fast path (description substring-matches an element — the common case) returned BEFORE the bonus section, so retrieved patterns were logged as "applied" but never changed any winner. The bonus now applies on the fast path too (both bonus methods are no-ops without RAG patterns/site scope, so RAG-off mode is untouched). Validation: (a) live `verify_production` A/B — decisive-rate 0% → 21% of applied bonuses, saucedemo execution identical (5 passed/1 skipped); the lone automationexercise difference was a generated-URL-assert flake with an identical click selector (not RAG-attributable); (b) local mock flip-correctness vs eval golden keys — 100% of golden AND learned fastpath flips landed on the golden-key element, lifting ecommerce resolution accuracy 53.8%→84.6% (golden store) and 46.2%→76.9% (learned store). A magnitude bump (5→25) was tried and rejected — near-inert on 3 of 4 sites, so speed didn't matter, the fast-path omission was the whole story. 1 new unit test (`test_rag_bonus_applies_on_haystack_fast_path`). Gates: 2876 passed, smoke 39/39, eval static 97.9% (RAG-off, unchanged), ruff + mypy clean.
- **AI-061 — opt-in production project-scoped RAG identity (2026-08-27)**: the learned-pattern store scoped only by `sha256(host[:port])`, so two projects on the same `localhost:PORT` bled patterns into each other (`lv_insurance` and a solo `ecommerce` run both map to `localhost:8781`). New opt-in `AITEST_RAG_SCOPE` env var participates in the RAG site identity (`scope:<value>`) at BOTH learn time (`src/rag_learn.py::effective_site_identity`) and resolve time (`PlaceholderOrchestrator`), so a pattern learned under one scope is only applied when the resolver runs under the same scope. Unset preserves the legacy host[:port] scoping (B-047) byte-for-byte — verified by `tests/integration/test_learning_loop_e2e.py` (asserts `site_hash("localhost:8784")`) plus 3 new unit tests (`tests/test_learning_impact.py`). End-to-end verification (real store + vector retrieval + the AI-059 Deliverable-2 usage trace) confirms a scoped learned pattern is retrieved, applied as a +5 bonus to the winning element, and recorded as `eligible/matched/bonus` in `rag_diagnostics.jsonl`, while staying invisible under a different scope or legacy host:port. Gates: 2874 passed, smoke 39/39, eval static 97.9% (no regression), ruff + mypy clean.
- **AI-059 Deliverable 2 — RAG resolver usage trace (2026-08-27)**: retrieved RAG patterns are now observable as *applied*, not just *retrieved*. `src/rag_retriever.py::RAGRetriever.pattern_usage()` returns one record per retrieved `RetrievedPattern` reporting `eligible` (site-gate pass for this run's site identity), `matched` (selector hit the winning element), and `bonus` (points contributed) — mirroring the applied `PlaceholderScorer` logic; `scoring_bonus_for` now delegates to it (byte-identical applied bonus, first-match-wins preserved, applied `PlaceholderScorer` path untouched). `PlaceholderOrchestrator` emits an opt-in `usage` line to `AI059_RAG_DIAGNOSTICS_PATH` per resolved placeholder (correlated to the retrieval line by `action`+`description`) via a new `_write_rag_usage_diagnostic` staticmethod inside `try/except` so diagnostics never affect resolution. 1 new test (`tests/test_learning_impact.py::test_pattern_usage_reports_eligible_match_and_bonus`).
- **AI-045 §8.4 #5 — Screenshot credential redaction (2026-08-25)**: evidence artifacts never persist typed secrets. New `src/credential_redaction.py`: sensitive-field classification (locator string → live attrs (`type="password"`, id/name/placeholder/aria-label/data-testid via broad token regex, snake_case-safe lookarounds, camelCase pre-split) → associated `<label>` text); `EvidenceTracker.fill()` replaces sensitive values with `***REDACTED***` in the sidecar JSON AND any label quoting them (both success + failure paths); every evidence screenshot is wrapped in `masked_screenshot_page` (blanks filled password/API-key inputs via JS for the capture window, restores originals in `finally`); `navigate()` strips basic-auth userinfo from recorded URLs. 51 unit tests + 1 real-browser integration test (`tests/test_credential_redaction.py`); verified live on saucedemo — sidecar shows `***REDACTED***`, zero secret hits across all evidence JSONs, pixel-level check confirms dots absent from captured PNGs with values restored. Gates: suite **2844 passed / 0 failed**, smoke 39/39, eval static 97.9%, ruff + mypy clean.
- **AI-045 #4 — PDF OCR wiring + doc-chunk dedup key (2026-08-24)**: two High commercial-readiness gaps closed in the production ingest path (audit §8.2). **OCR**: `ingest_pdf`/`ingest_pdf_directory` gained a page-scoped `ocr_fallback` hook — an image-only page is sent to the configured OCR backend instead of being silently skipped; `OcrBackend.parse_page` added (UnlimitedOCR rasterises the single page at 300 DPI); `scripts/rag_ingest.py --pdfs` now consults `get_ocr_backend()`. With no OCR backend, skipped pages log a WARNING (was silent `info`) hinting at `OCR_BACKEND=unlimited-ocr`. **Dedup**: `DocChunk.dedup_key` = `sha256(source \x00 heading_path \x00 normalised_text)`; `RAGStore.add_docs` now returns `(inserted, skipped)` and skips chunks whose key already exists (re-ingestion is idempotent); new `--prune-dupes` CLI flag removes existing duplicate doc rows (keeps lowest id per group). 10 new tests (OCR wiring uses real PyMuPDF PDFs + a plain callable OCR hook, no GPU); gates (fitz installed for CI parity): full suite **2793 passed / 0 failed**, smoke 39/39, eval static 97.9% (no regression), ruff + mypy clean, coverage 70%.

### Fixed
- **AI-051 — B-021 page-state URL assertions now assert the OBSERVED landing page (2026-08-23)**: a page-state ASSERT ("home page loaded", "logged in") was resolved by keyword-matching the description against scraped pages; when the preceding action navigated to a page that was never scraped as a *description* match (e.g. saucedemo Login `home → /inventory.html`), the keyword match fell back to the **base/starting URL** → `expect(page).to_have_url("https://www.saucedemo.com/")` after a login click, which always fails (saucedemo redirects to `/inventory.html`). The B-021 branch in `placeholder_orchestrator.py::_replace_placeholders_sequentially` now consults the AI-052 observed trail: when the trail evidences the step's landing page differs from the keyword-inferred one, it emits the observed `to_url` (a browser fact). Gated on `obs is not None and not diverged and pending_evidence is None` so no-trail (back-compat) mode is unchanged. 4 new tests (`tests/test_ai051_page_state_url.py`); verified in production — `verify_production saucedemo` execution now **5 passed / 1 honest skip / 0 failed / 0 different-page errors** (was 4/1F/1S), `test_01_login` asserts `/inventory.html`. Eval static 97.9% (no regression); 2744 passed / 1 skipped.
- **AI-053 — `uat.py --all-sites --save` persisted only the last site (2026-08-23)**: found during the AI-052 S6 ship — `results.append(site_result)` sat outside the site loop in `main()`, so with `--all-sites` the saved JSON and the OVERALL summary both reflected only the last site even though both ran (the S6 automationexercise 12/13 result survived only in the run log). The append is now inside the loop and the OVERALL line aggregates across all sites via `summarize_results()` (`--compare` already merged multi-site baselines by `site_id`). 5 new tests in `tests/test_uat.py`, including an AST guard that pins `results.append` inside the loop so the dedent regression can't return silently.
- **AI-052 S5 — ARIA role gate in fast passes, penalty-first (2026-08-23)**: `element_matcher` fast passes (0/D/1/2) no longer let a heading/textbox/status-region win a CLICK on text overlap — role-contradicted matches are deferred so the deeper role-aware passes compete, with the deferred candidate kept as a last resort (never a hard filter). `computed_role` authoritative; FILL/ASSERT untouched. Resolver-mode A/B: 97.9% = 97.9% (zero golden regressions); eval full FP rate identical to historical same-model leg. 9 new tests.
- **AI-052 S4 — keyword-URL guessing deleted; evidence-only transitions (2026-08-23)**: `src/url_inference.py` rewritten — `infer_next_page_url` derives transitions only from the clicked element's own `href`; the login/checkout/continue/finish/transfer/pay keyword branches and `_find_discovered_url` are gone. `journey_scraper._infer_url_from_description` (keyword → fabricated path + HEAD probes) replaced by `_match_discovered_url`, which navigates only to already-scraped pages. Open question #3 answered: eval static **97.9% unchanged**. Both verify sites unchanged vs post-S3 (zero different-page errors).
- **AI-052 S1–S3 — Observed Transitions: the resolver no longer guesses next-page URLs (2026-07-23)**: generated tests could emit a locator for a page the test isn't on (repro: saucedemo `click('#item_4_title_link')` → `?id=4`, then an add-to-cart button that only exists on inventory → runtime `_LocatorNotFoundError: element exists on a different page`). Root cause: the journey scraper OBSERVES real transitions (`page.url` after each step) but the resolver discarded that and re-guessed via keyword/href inference, falling back to matching against ALL scraped pages. **S1** — `JourneyScraper` now captures a typed `ObservedTrail` (one factual `ObservedStep` per step: from/to URL, navigated, scraped, proven selector; round-trips through the subprocess boundary). **S2** — trail plumbed into `_replace_placeholders_sequentially`. **S3** — the resolver consumes it: strict scoping to verified pages (never all-pages fallback), honest `pytest.skip` for evidenced/unknown pages, GOTO resolves from observed landings, plus four production-driven mechanisms (pending-evidence for emitted href-navigations, divergence-aware replay of the scraper's proven selectors, proven-static navigation intent, divergence latch). Gates: 2726 passed / 1 skipped, smoke 39/39, eval static **97.9% (no regression)**, ruff + mypy clean; `verify_production`: **zero different-page locator errors on BOTH saucedemo and automationexercise** (saucedemo 4 passed/1 failed — remaining failure is AI-051, out of scope; automationexercise 6/7). 19 new tests in `tests/test_resolver_observed_scope.py`; session records under `docs/sessions/`.

### Changed
- **Repo hygiene audit (2026-08-21)**: dissolved `scratch/` — model-benchmark material (A/B analysis docs, GGUF quantization confound, lm-eval GSM8K results, leg manifests, server logs, probes) relocated to the sibling `llm-benchmarks` repo (`imported_from_tancat_scratch/`, since versioned in `lacattano/llm-benchmarks`); stale debug scripts deleted (superseded by `scripts/debug/`); `scratch/` now gitignored so agent scratch can never be tracked again. Renamed `scripts/3d map` → `scripts/map_3d` (directory names must not contain spaces) and regenerated `docs/nodes.csv` / `docs/links.csv` / `scripts/3d_map_data.json` against the new path (audit clean: 0 dead nodes/links). Deleted AI-029 workspace test residue (`before/`, `custom-ws/`, `explicit/`, `tier2-demo/`) and fixed `tests/test_storage.py` to use `tmp_path` roots so workspace-isolation tests stop creating dirs at repo root. Removed path-escaping bug artifacts (`./~/`, `./C:`) created by mis-quoted shell commands. Gates: 2691 passed / 1 skipped, smoke 39/39, ruff + mypy clean.

### Added
- **Phase 6 6b — embedding model stamp + reindex (2026-08-17, BACKLOG AI-045 #2)**: changing the embedding model can no longer silently corrupt retrieval. **`src/rag_store.py`** — `EmbeddingMismatchError`; `SentenceTransformerEmbedder.identity` (`model@dim`); `MilvusLiteBackend` writes an embedder-stamp sidecar (`<db>.embedder.json` — Milvus Lite 3.0 doesn't persist collection descriptions, so the spec's sidecar option) at creation and **verifies on every open**: dimension mismatch always refused; embedder mismatch refused (cosine similarity is meaningless across embedding spaces); a legacy no-sidecar store is accepted + migrated forward only for the default embedder (the only model that could have built it). `RAGStore` cross-checks its *actual* embedder identity before every operation (backend constructor identity ≠ embedder identity can no longer smuggle mismatched vectors). **`scripts/rag_ingest.py --reindex`** — rebuilds the store from the bundled golden pack + docs (deletes store + stamp, resets learned patterns, rewrites stamp + seed marker), the documented fix in every refusal message. **Refusal is loud, never silent**: `RAGRetriever` logs an ERROR with the reindex command (not the generic degradation warning), the orchestrator's bundled-seed catch special-cases it, and the CLI prints a clean error instead of a traceback. Construction sites pass `embedder_identity` (`rag_bundled.build_default_store`, `rag_ingest.rebuild_store`, orchestrator, eval resolver). 13 new tests (stamp write/reopen/mismatch/legacy-migration/RAGStore cross-check/CLI + retriever mismatch); gates: full suite **2654 passed / 4 skipped**, smoke 39/39, eval static **97.9% (no regression)**, ruff + mypy clean; legacy migration verified against the real store (145 entries → stamped `all-MiniLM-L6-v2@384`). Spec §6 6b marked shipped.
- **Phase 6 6a — SSRF guard + egress audit (2026-08-17, BACKLOG AI-045 #1)**: the "no data leaves your deployment" claim is now literally true. **`src/url_guard.py`** (new) — resolve-and-classify SSRF guard: http/https scheme restriction; unconditionally-blocked link-local (incl. the `169.254.169.254` cloud-metadata endpoint), unspecified/broadcast/multicast/IETF-reserved ranges, and the special-purpose registry (0.0.0.0/8, 100.64/10, 192.0.0.0/24, 198.18/15); loopback blocked by default in CI/team contexts but **default-ON for local/UI use** (`AITEST_ALLOW_LOOPBACK`, the mock-site family + local dev depend on it and loopback never leaves the box); private RFC1918/ULA blocked by default with `AITEST_ALLOW_PRIVATE_NETWORKS=1` opt-in for internal staging. Per-host DNS cache + a Playwright `page.on("request")` handler so redirects and sub-resources are re-checked at request time. **Wired at every navigation entry point**: orchestrator URL intake (fail fast), `src/scraper.py` (pre-subprocess + in-subprocess), `src/stateful_scraper.py` (per-URL + session seeding), `src/journey_scraper.py` (starting URL + `_navigate_to` + the out-of-browser `httpx.head` candidate probe), and `scripts/ci_generate.py` **under** the Phase 7 danger-zone check (new `--allow-private-networks` flag; `--danger-zone` can never unblock link-local). **`scripts/audit_egress.py`** (new) — static gate that scans the product runtime for outbound-HTTP primitives and fails if any call site isn't user-configurable/target-derived (verdict 2026-08-17: 143 files, 13 sites, 0 flagged — all go to the customer's LLM endpoint or the guarded target site); wired into `scripts/smoke.py` Gate 0 (now 39/39) and CI. **`docs/security/egress-audit.md`** (new) — the public verification note with the full call-site table. 43 new tests (28 guard unit incl. IPv4-mapped-IPv6 smuggling + CGNAT/reserved ranges, 9 audit, 3 scraper integration, 3 ci_generate guard-composition). Gates: full suite **2641 passed / 4 skipped**, smoke 39/39 (incl. the new egress gate), ruff + mypy clean, fake-LLM E2E green (full generate→run→evidence through the guarded pipeline). Spec §6 6a marked shipped.
- **SaaS launch research + Phase 6 restructured as a two-part plan (2026-08-17)**: `docs/plans/RESEARCH_SAAS_AND_LAUNCH.md` (new) — research log + decision record for the commercial-launch gaps the Phase 6 checklist didn't cover. Decisions: **D1 BYO-LLM** (deployer's LLM does the work — local for one user, vLLM/self-hosted for a team, cloud API key for teams without GPU; TanCat does not host LLMs), **D2 per-company deployment is the v1** (customer's infra/LLM/data; team shares one workspace; no cross-user account linking), **D3 true multi-tenant SaaS is tier 2** (the three learned stores — `run_results.sqlite`, RAG vector store, flow memory — are global per machine today and are the cross-tenant leak vector if strangers shared a platform), **D4 credentials** (CI reads secrets from the CI platform's store via env vars; never persisted where avoidable; role scoping is per-story/per-test by declaration — TanCat never auto-selects credentials and the learning stores never learn which login unlocks which element). ROADMAP Phase 6 restructured: Part 1 = per-company deployment (BYO-LLM onboarding/health probe, team auth on the customer's instance, offline ed25519 license key, credential policy, no-egress guarantee, SSRF blocklist — 3-4 sessions) with a **spec prerequisite** (`FEATURE_SPEC_phase6_saas.md`, not yet written — owns the free-tier limit research + license key design); Part 2 = true multi-tenant SaaS (per-tenant isolation, S3, sandbox — 8-12 sessions, deferred until Part 1 is selling). New roadmap item **12c — Upgrade Path** (regenerate old packages after pipeline changes: manifest versioning + pipeline fingerprint, regenerate-to-new-package safety default, eval-harness drift report — 2-3 sessions). Phase 7 recorded: **AI-039 rename is a prerequisite before first PyPI publish / launch batch** (Action owner reference + package name + repo name are one coordinated decision). Observability/concurrency explicitly out of v1 scope (customer ops + customer's own LLM are the boundaries).

### Fixed
- **Thinking-budget collapse: the real root cause of `got=0` retry loops and resolution timeouts (2026-08-19, BACKLOG AI-050)**: Qwen3.6/3.8 are thinking models — their `reasoning_content` phase ran BEFORE any answer and routinely burned the entire 4096-token `max_tokens` cap, so `finish_reason=length` came back with `content=''` (measured: 3.8 2/3 calls, 3.6 2/2, temp=0.0). The provider reads only `message.content` → empty skeleton → the `expected=N, got=0 → Retrying` loops blamed on "the 156k config", and on resolution calls the 160–310s thinking phase blew the old 45s timeout (AI-049's pollution source). Run C's "3.6 tolerates the config" was retry luck; 3.6 collapses identically at the raw-call level. **Fix — explicit, visible, never silent**: `LLMProvider.complete()` gains `enable_thinking: bool | None` (`None` sends nothing — the model/server default is never silently overridden; `True`/`False` → OpenAI-compatible `chat_template_kwargs`, proven effective per-request on llama.cpp); `LLMClient` threads + logs it per call (`thinking=off|on|default`); the two proven-broken structured call sites opt out explicitly (`TestGenerator._generate_skeleton_single_call`, `SemanticCandidateRanker` constructor default); LangGraph stages keep the model default until measured per-stage. `eval_runs` gains `thinking TEXT` (ALTER-migrated; linear=`off`, graph=`model-default`) so no future session is misled about what a number was measured with. `/no_think` in-band rejected (model ignores it); 16384 max_tokens works but 17× slower. Measured: skeleton calls 160–310s → ~15–18s with content every time on both models. Side finding: draft-MTP spec decode is the residual non-determinism source (spec ON: token-level jitter @14.8s/gen; spec OFF: byte-identical @32.4s/gen) — A/B runs spec OFF by decision. 14 new tests; gates: full suite **2691 passed / 1 skipped**, smoke 39/39, eval static 97.9% (exit 0), ruff + mypy clean. Touched three protected files with minimal additive changes (`src/llm_providers/__init__.py`, `src/llm_client.py`, `src/test_generator.py`) — flagged for review.
- **Resolution LLM timeout: 45s hard-code + silent failure removed (2026-08-18, BACKLOG AI-049)**: the resolution LLM calls in `src/semantic_candidate_ranker.py` (single + batch) were hard-coded to `timeout=45` and wrapped in `except Exception: return None` — on a loaded server the call times out, `generated_locator` comes back `None`, the placeholder silently scores 0, and flat-0% eval sites appear with no log. This polluted every eval leg in the 2026-08-18 model A/B investigation (27 resolution timeouts in the 3.6 leg, 7 in the 3.8 leg — the delta was timeout noise, not model skill). Now: `DEFAULT_RESOLUTION_TIMEOUT = 120.0` + keyword-only `timeout` on the ranker; `_is_timeout_error()` walks the exception cause chain (`LLMClient.generate` wraps provider errors in `RuntimeError`); both call sites log a WARNING naming the timeout, elapsed-vs-limit, and the affected action/description(s) — `None`→0% is never silent again. Plumbed keyword-only with defaults through `ElementMatcher` → `PlaceholderOrchestrator` → `TestOrchestrator`; `scripts/eval/eval_runner.py` passes it explicitly at both orchestrator constructions (the eval is the consumer that measures resolution). Deliberately not an env var or CLI flag — constant-in-code keeps A/B legs on identical conditions. 8 new tests; gates: full suite **2677 passed / 1 skipped**, smoke 39/39, eval static 97.9% (exit 0), ruff + mypy clean.
- **Linear pipeline sampling config pin + eval_runs instrumentation (2026-08-18, BACKLOG AI-048)**: the linear path (default UI/CLI/eval) called `LLMClient.generate()` with `temperature=None` → providers omitted the field → **the server's own default (1.0 on llama.cpp launches) silently governed skeleton generation** — max entropy at the exact stage where determinism matters, and an unrecorded confound that hampered the AI-046 3.6-vs-3.8 A/B (both models' runs used each launch's server-default sampling; the 3.6 launch's `/props` was never captured). **`src/llm_client.py`** — `llm_temperature_default()` reads `AITEST_LLM_TEMPERATURE` (default `0.0`, clamped 0–2, invalid → warn + 0.0, matching the LangGraph agents' proven `temperature=0` determinism); `_complete_sync()` now substitutes the pin when a caller passes None (agents pass `0` explicitly — unchanged); `generate_test()` gained a `temperature` pass-through. **`scripts/eval/eval_runner.py`** — `eval_runs` gains `temperature_sent REAL` + `server_defaults TEXT` (ALTER-migrated for existing DBs; legacy rows stay NULL), recorded by `_sampling_identity()`: resolved delivered temperature (graph → 0.0, linear → env-or-0.0) + a best-effort `/props` snapshot (temperature/top_p/top_k/seed/repeat_penalty/n_ctx). `.env.example` documents the var. Audit finding preserved: `/props` `speculative.types` is the per-request override default, NOT the serving config — `/slots` reports the truth (slot 0: `speculative: True`); per-agent/per-stage model+settings selection never existed (multi-provider spec §13 Q4 stayed centralized). 7 new tests; gates: full suite **2668 passed / 1 skipped**, smoke 39/39, ruff + mypy clean. Next: controlled A/B re-run.
- **Eval harness mock-server bug — mock stories 0% in regenerate mode (2026-08-17, AI-047)**: `EvalRunner._ensure_mock_server` served ONE directory for the whole run (the first localhost dataset's `mock_dir`; legacy eval-005 has none → repo root), so `mock_sites/ecommerce|banking` URLs 404'd during `--regenerate` — eval-006/007/008 scored 0% and dragged the headline resolution down (3.8 A/B: 31.2%). Golden keys reference root-relative URLs (`/cart.html`) and ecommerce + banking both use `/index.html`, so one root cannot serve both — the server is now (re)started **per story** in both the regeneration and execution phases: `_build_mock_dirs()` maps story_id → served dir (declared `mock_dir` resolved against the repo root, legacy → repo root; injectable root for tests), `_ensure_mock_serves()` swaps the :8781 server, `run_full_validation` gained an `on_story` hook. Also fixed a latent repo-root derivation bug (`dataset_dir.parent.parent` landed on `scripts/` — now module-path-based) and the `eval_runs.model` gap that hampered the 3.6-vs-3.8 comparison (`_loaded_model_identity()` records provider/model). 4 new tests (`tests/test_eval_runner_mocks.py`); verified end-to-end: each mock family serves at root (ecommerce/banking/lv all 200). Static eval unchanged at 97.9%.
- **`file://` mock-scrape tests broken by the 6a SSRF guard (2026-08-17, found by the full-suite run)**: 3 integration tests in `tests/integration/test_pipeline_end_to_end.py` scraped the local `mock_insurance_site.html` fixture via a `file://` URL — the new guard correctly refuses that scheme, silently turning 3 previously-passing tests into skips ("SSRF guard refused ... scheme 'file' is not http/https"). The fixture is now served over loopback HTTP via a lazy `ThreadingHTTPServer` on `127.0.0.1:0` serving `generated_tests/` (the same shape as the product's own `scripts/serve_mock_site.py`); `mock_insurance_file_url()` renamed `mock_insurance_url()`. All 5 call sites updated (incl. the 2 slow/integration pipeline tests). Result: those tests pass again; full suite **2657 passed / 1 skipped** (the single skip is the pre-existing PyMuPDF-not-installed one).
- **`PipelineRunService` directory-target evidence dir (2026-08-16)**: `run_saved_test` derived the suite-chain evidence dir from `Path(saved_path).parent` — correct for a test FILE (the UI/CLI always pass files) but WRONG for a package DIRECTORY: it landed in `generated_tests/evidence/` and chained stale sidecars into flow memory. Now resolves the package dir explicitly (`saved if saved.is_dir() else saved.parent`), so both target shapes point at the package's own `evidence/`. This removes the learning-loop E2E's workaround — it no longer parks `generated_tests/evidence/` nor calls `learn_suite_flows` manually (the fixed hook already chains the right dir; the manual call was doubling hit counts, which the test's `hit_count == 1` assertion caught). +2 unit tests (directory target → `<pkg>/evidence`; file target → `<pkg>/evidence` unchanged). Verified: learning-loop E2E 4/4 (real mock site + real conftest + real run service).
- **Product `Dockerfile` — three latent flaws fixed (2026-08-15, the Phase 7a-tail "latent issue" noted in BACKLOG)**: the product image now adopts the same fixes the `Dockerfile.action` image already proved: (1) **empty-project-wheel flaw** — the old single `uv sync` ran before `COPY . .`, so the project wheel (hatchling `packages = ["src", "cli"]`) built from a context with no source; now a two-stage sync (`--no-install-project` for deps with a warm cache, full sync after the repo copy); (2) **`~/.cargo/bin/uv` path** — now `UV_INSTALL_DIR=/usr/local/bin` + `UV_PYTHON_PREFERENCE=only-system` (the venv's python never symlinks into uv's managed-CPython dir, which doesn't exist in the runtime stage); (3) **python version mismatch** — the old `mcr.microsoft.com/playwright/python:v1.50.0-jammy` base ships python 3.10, which cannot run the repo's PEP-758 syntax (`requires-python >= 3.14`); runtime base is now `python:3.14-slim` with Chromium installed from the venv's own playwright (uv.lock → 1.61.0, version-matched, never the image's stale 1.50 bundle). Verified locally: clean build, in-image imports of `src`/`cli`/`streamlit`/`playwright` OK on python 3.14.7, chromium present at `/ms-playwright`, Streamlit container boots (`/healthz` OK, no import errors in logs). `docker-compose.yml` unchanged (still builds this Dockerfile).

### Added
- **Phase 7 7d — `learn: true`: flow-memory learning in CI (2026-08-15, the last Phase 7 tail)**: CI now behaves like the UI/CLI (the settled design — no re-grill): on a green generate-and-run with `learn: true` the action exports `RAG_ENABLED=0` (**flow memory only in CI** — the RAG leg needs an ~80 MB embedder download per runner; documented in `docs/ci.md` §8b) and points the lazy storage singleton at the runner-mount workspace (`AITEST_STORAGE_ROOT`/`AITEST_WORKSPACE` env overrides in `src/storage.get_storage()` — backwards compatible, UI/CLI call `init_storage` explicitly and are unaffected). The generated package's conftest teardown writes within-test flows into `<workspace>/evidence/flow_memory.json`; the entrypoint reports the learned counts (`flow_store`/`flow_patterns`/`flow_sites` outputs + a `learning:` log line) on green runs. Saturation is expected and documented: a stable package vs a stable site learns most patterns on the first green run; later runs dedup + reinforce hit counts (value = consistency + first-run seeding for NEW stories on the same site — why it is opt-in and default off). Suite chains (the UI/CLI post-run hook) are deliberately not learned. `generated_tests/conftest.py`: the RAG-learning leg is now gated on `RAG_ENABLED != "0"` (CI never pulls the embedder); the flow-memory leg stays always-on for passing steps. **`action/flow_memory_stats.py`** (new) — platform-neutral store-stats helper for the learned-count reporting (missing/corrupt store = zeros, exit 0). **Cache**: `.github/workflows/ci-cd-action.yml` gains a branch-scoped `actions/cache` key (`ai-testgen-flowmem-${{ github.ref }}`, restore/save) + a `learn: true` self-test step (single `-k` test; stub asserts store file + `flow_*` outputs + no `rag_store.db`); `ci/gitlab-ci.template.yml` gains `AITEST_LEARN`/`INPUT_LEARN` + the store in the branch-scoped cache `paths`; `action.yml`'s `learn` input description updated (was "NOT IMPLEMENTED"). **Selftest 39 → 50 gates** — learn seed (green run persists the store; `flow_patterns` ≥ 1; RAG leg off) + learn restore (a pre-seeded marker survives a re-run and `flow_patterns` = 2 = marker + newly-learned — merge, not overwrite; the branch-cache contract). +10 unit tests (storage env overrides x3, stats helper x7). Gates: local selftest **50/50**, full suite **2597 passed / 1 skipped**, ruff + mypy clean. The Phase 7 backlog tail is closed.
- **Phase 7 7c — GitLab parity (2026-08-15, Phase 7 milestone complete)**: the §5.5 platform seam now has both skins — GitHub (7a/7b) + GitLab. **`ci/platform/gitlab.py`** — the GitLab REST adapter mirroring github.py (MR notes under `/projects/:id/merge_requests/:iid/notes`; find-by-marker → **PUT** edit-not-duplicate (GitLab edits are PUT, not PATCH); `PRIVATE-TOKEN` auth; numeric id **or** URL-encoded `group%2Fproject` paths; injectable base URL + `client_from_env` honoring both GitLab-runner-native `CI_*` vars and explicit overrides; `--latest-command` CLI mode for the slash job). **`ci/gitlab-ci.template.yml`** — include template with the same three modes (generate-only / generate-and-run / run-existing): `ai-testgen:build-image` (docker dind → project registry), `ai-testgen:compute-key` (the §7 `action/cache_key.py` formula via a dotenv report feeding `cache:key` — one source of truth, GitLab evaluates cache:key after collecting needs' artifacts), `ai-testgen:run` (the action's `/entrypoint.sh` with the image entrypoint neutralised via `entrypoint: [""]`; underscore `INPUT_*` variables because GitLab names can't contain hyphens; `GITHUB_WORKSPACE: $CI_PROJECT_DIR`; `artifacts:reports:junit` → GitLab's native MR test UI; cache `key`/`fallback_keys` mirroring the GitHub run-id/branch patterns), and a **manual** `ai-testgen:slash-command` job (fetches the newest `/adapt`/`/ignore` MR note via `--latest-command`, posts the reply; manual-first is arguably safer than GitHub's auto-trigger — free tier has no note-event pipeline trigger). Protected-environment approval gate for the danger zone (`ai-testgen-prod`). **`action/entrypoint.sh`** gained `detect_platform` (explicit `platform` input or `GITLAB_CI` auto-detect) + a gitlab branch in `post_comment` (same marker, same edit-not-duplicate contract); **`action.yml`** gained the `platform` + `gitlab-*` inputs. **`docs/ci.md`** — modes, when to adapt (shared vs isolated envs), ignore-list format, cache, danger-zone approvals, security, GitLab specifics. **Self-test 26 → 39 gates**: host-side `MockGitLabAPI` — MR note POSTED with §6 shape + `PRIVATE-TOKEN` + URL-encoded path (cache-hit reuses the GitHub gate's seeded package — no duplicate ~2.5 min generation), slash `/adapt` (sabotaged cached package → keep) + `/ignore` replies as notes (3 notes total). +14 unit tests (mock GitLab API server: idempotent create→PUT-edit, find-by-marker, URL-encoded paths, latest-command, env mapping). Gates: **local selftest 39/39**, full suite **2587 passed / 1 skipped**, smoke 38/38, ruff + mypy clean. **Real GitLab.com gate: PASSED 2026-08-15** (14/14 live checks on a real GitLab.com project — push pipeline + junit 8/8 + cache miss, MR §6 note posted + edited-not-duplicated, cache hit on re-run; `scripts/ci_gitlab_real_project_test.py`). Roadmap Phase 7 → `[x]`.
- **Phase 7 7b — generate-and-run: PR comment, cache, slash-commands, verified adaptation (2026-08-15)**: the CI action's full loop per spec §8/7b. `generate-and-run` mode in `action/entrypoint.sh` (generate-or-cache-hit → pytest referee → evidence JUnit → report + flaky markers → §6 PR comment payload + idempotent posting; `adapt: true` repo-level opt-in with a referee-exit override; `learn: true` fails fast, never a silent no-op). **Cache (spec §7)**: `action/cache_key.py` — one source of truth for key = `sha256(story+url+model+provider+PROMPT_FINGERPRINT)` (pure stdlib, shared by the workflow's `actions/cache` steps and the action's internal cache-dir check; fingerprint bump invalidates all cached packages — the AI-042-F4 lesson); emits `cache_key`/`cache_hit` outputs, seeds `<cache-dir>/packages/<key>/` on miss, reuses on hit, `cache: false` to disable. **PR comment (spec §6)**: `ci/platform/github.py` — the GitHub surface behind the §5.5 platform seam (find-by-marker → edit-not-duplicate, reply posting; stdlib urllib; injectable base URL so tests/selftest point at a mock API); `action/report.py` gained Site/Model context + the flaky block; the entrypoint always writes `comment.md` and posts only when `repo`+`pr-number`+`github-token` inputs are present. **Verified adaptation engine (spec §9.6)**: `action/adapt.py` — parses LocatorNotFound-class failures (Playwright `locator('…')` AND evidence_tracker `Locator '…' not found` shapes), locates the source step, re-resolves the semantic label with the product's own `PageScraper`+`PlaceholderResolver` (no LLM), patches, re-runs only that test, keeps only if assertions still pass (reverts otherwise), emits `adaptation.json` — assertion failures never auto-adapted, CI never mutates silently. **Slash-command loop**: `scripts/ci_slash_commands.py` (platform-neutral parse + reply rendering; `/ignore` renders the exact `.ai-test-ignore.yml` entry with the required reason) + internal `slash-command` mode + `.github/workflows/ci-slash-commands.yml` (`issue_comment` trigger, `pull-requests: write`, fork-PR guard, branch-cache restore). **Self-test extended**: ci-cd-action.yml adds cache restore/save + generate-and-run + sabotage→`/adapt` + `/ignore` phases (stub asserts, no real PR); `scripts/ci_action_selftest.py` is now 28 gates incl. a host-side mock GitHub API (real POST/PATCH traffic via `host.docker.internal` — verified 1 comment, edited not duplicated, adapt reply, ignore reply = 3 total). +45 unit tests; gates: **2571 passed / 1 skipped**, smoke 38/38, ruff + mypy clean.
- **Phase 7 7a tail — Docker action + hermetic self-test (2026-08-14)**: the CI/CD action is now packaged and self-tested. `action.yml` at the repo root (Docker action metadata; inputs mode/story/tests/url/workspace/pom/provider/model/llm-base-url/llm-api-key/credential-profile/ignore-file/danger-zone/allowed-domains/pytest-args + internal `self-test`), `Dockerfile.action` at the repo root (uv-built 3.14 venv on python:3.14-slim; Chromium from the venv's own playwright so the browser matches uv.lock; fixes the three latent product-image flaws — empty project wheel from `uv sync` before `COPY src`, `~/.cargo/bin/uv` path, and the playwright/python image's python 3.10 vs the repo's PEP-758 syntax), `entrypoint.sh` (thin orchestrator over the driver: `generate-only` + `run-existing`; `generate-and-run` fails fast with a clear message — arrives in 7b; GitHub env surface isolated per spec §5.5 platform seam), `report.py` (JUnit → counts + **repair-candidate marking**, 7a scope: marking only, no adaptation; writes report.json + report.md in the §6 comment shape), `export_evidence_junit.py` (AI-028 evidence sidecars → enriched JUnit). `scripts/ci_generate.py` gained `--storage-root` so the action can write under `$GITHUB_WORKSPACE` (artifacts persist to the runner). `.github/workflows/ci-cd-action.yml` — hermetic self-test (mock site + fake LLM boot inside the container): generate-only → run-existing, with stub-step asserts (exit codes, driver JSON contract, junit + evidence-junit well-formedness, report payload shape; no real PR). `scripts/ci_action_selftest.py` — the same 9-gate self-test locally via Docker; verified on this machine (generate-only: 8 tests / 137s; run-existing: 8 tests 6 passed 2 skipped; junit + evidence junit + report all green). `.dockerignore` hardened: the action build context measured 7.3 GB → ~20 MB — heavy historical output (generated_tests/**.venv/graphify-out), docs/tests/dev tooling, and root strays (nul/run_results.sqlite/streamlit logs) excluded; runtime reads (src, scripts, assets/logo, .streamlit, generated_tests/conftest.py) verified and kept. 16 new unit tests (storage-root x2, report x11, evidence-junit x3); gates: 2526 passed / 1 skipped, smoke 38/38, ruff + mypy clean, eval static 97.9%.
- **Phase 7 CI/CD — spec + 7a core (2026-08-13)**: `docs/specs/FEATURE_SPEC_phase7_ci_cd_integration.md` — drafted + fully grilled (all decisions closed: referee-by-default CI with no self-healing, verified adaptation offered post-failure via `/adapt` + tool link, `.ai-test-ignore.yml` with a **required-`reason` anti-rug rule**, danger-zone allow-list Option C, two-stage Action repo [in-repo now → thin public repo + PyPI at launch], trigger scope excluding fork PRs, flaky from cached per-branch history, GitLab parity in 7c). 7a core: `scripts/ci_generate.py` — the product's first **headless generation driver** (exit codes 0/1/2, `--json`, AI-029 workspace isolation, danger-zone allow-list, credential profiles; calls the same `ui_pipeline.run_pipeline()` the UI/CLI use); `scripts/fake_llm.py` — OpenAI-compatible fake LLM routing conditions / per-condition skeleton fragments / semantic responses so the full pipeline runs offline against the mock sites (hermetic, never decays); `src/ci_ignore.py` — parse/validate/match for `.ai-test-ignore.yml`. 28 tests (12 ignore + 16 driver incl. a slow-lane E2E: fake LLM + ecommerce mock → package written, workspace-isolated, 8 tests). Gates: 2510 passed, smoke 38/38, eval static 97.9%. 7b, 7c pending — tracked in BACKLOG.

### Fixed
- **B-047 parent-side RAG sweep — verified + wired-in guards (2026-08-15)**: the fix (shipped 2026-08-11, `e03ee0e`) was live-verified against the exact bug scenario — parent process holds the Milvus-lite store → subprocess open blocked (as diagnosed: `DataDirLockedError`-class) → parent-side sweep `learn_from_evidence_sidecars()` of 5 passed ecommerce-mock sidecars learned 9 new + 1 repeat in-process (store 13 → 22 learned). New static guards in `tests/test_script_hooks.py` keep the sweep wired in `scripts/synthesize_stories.py` (presence + evidence-dir target + per-site loop placement) — a deleted sweep would otherwise go unnoticed since the script isn't exercised by the unit suite. Full suite 2599 passed / 1 skipped.
- **`PipelineArtifactWriter` now storage-aware (2026-08-13, found during Phase 7 7a)**: the `output_dir="generated_tests"` default silently bypassed AI-029 workspace isolation for the UI path — named workspaces wrote the generated package to the repo root instead of the workspace. Now resolves through `get_storage().generated_tests_dir()`; default-workspace behavior identical (full suite green).
- **Learning-loop E2E test — BACKLOG 🆕 item shipped (2026-08-13)**: `tests/integration/test_learning_loop_e2e.py` — one integration test proving the AI-042 learning loop end-to-end with REAL components (no mocks-of-mocks): the mock site is brought up as part of the automation (`MockServer` port 8784, `mock_sites/ecommerce`), a tiny 4-test generated package runs under the REAL `generated_tests/conftest.py` via `PipelineRunService` (persist=True), conftest teardown learns within-test flows, the post-run hook chains suite flows, `evidence/flow_memory.json` gains exactly the designed patterns (7: 4 within-test + 3 suite chains), a re-run dedups + bumps hit counts, and a follow-up orchestrator GOTO/URL step-2.5 resolution resolves from the learned flows. Hermetic: real store snapshotted/wiped/restored, legacy `generated_tests/evidence/` parked/restored, RAG-learning leg neutralised via package-local conftest (no `rag_store.db` writes / embedder download). Marked `slow`+`integration`+`subprocess` (excluded from default suite). Run: `pytest tests/integration/test_learning_loop_e2e.py -v`.
- **3D architecture map viewer (2026-08-12)**: `scripts/3d map/viewer.html` — self-contained Cosmograph renderer (ESM via esm.sh, `@cosmograph/cosmograph@1.4.2`, classic `new Cosmograph(container, config)` + `setData(nodes, links)` API); nodes colored by architecture layer, always-on labels for the top 60 hubs, hover labels, legend + stats. `generate_3d_map.py` fixes: `graphify-out` added to SKIP_DIRS (removed ~1,500 hashed AST-cache nodes: 4,681 → 3,167) and unmapped `.py` files now group as `Other` instead of `utility` (utility 1,965 → the real 14 src/ modules). Viewer gotchas documented: v1 key is `nodeLabelAccessor` (not `nodeLabel`), unpkg is CORS-blocked (use esm.sh), and the library sends a metrics ping to its Supabase on setData.
- **Testing hardening (2026-08-12)**: CI coverage gate (`--cov-fail-under=65` — measured 67%), page-state ASSERT flow-fallback tests (which uncovered + fixed a real gap: the sequential B-021 ASSERT branch never used flow memory, so URL asserts couldn't be flow-rescued in the real pipeline — now they can), and static guards (`tests/test_script_hooks.py`) that keep the `verify_production`/`synthesize_stories` suite-chaining hooks wired. Learning-loop E2E tracked in BACKLOG as a non-roadmap item.
- **AI-042-F2 — flow stats + prune in the Streamlit sidebar (2026-08-12)**: `SidebarConfig._render_flow_memory()` — "Flow Memory" subheader with `format_flow_stats_summary()` (patterns / sites / cross-site / suite chains — the F3 source split surfaces here) + two-step prune → `FlowMemoryStore.clear()`, mirroring the RAG "Learned Patterns" section; empty store degrades to a hint. Pure `format_flow_stats_summary` helper + 2 stubbed-UI tests. F-series complete (F1/F2/F3 shipped; F4 deferred).
- **AI-042-F3 — cross-test flow chaining (2026-08-12)**: `chain_suite_transitions()` + `FlowMemoryStore.learn_suite_flows()` chain adjacent fully-passing tests (name order) into GOTO transitions — terminal page of test N → entry page of test N+1, description = destination route name. Same-site pairs only (mixed evidence dirs never chain), home/no-movement dropped, pre-B-033 sidecars fall back to navigate values. `FlowPattern.source` (within_test/suite_chain) + `stats()` split. Wired into all run paths: `synthesize_stories.py` sweep, `PipelineRunService.run_saved_test` (UI + CLI), `verify_production.py`. Re-seed: 24 new suite-chain patterns (89 → 113, cross-site 5 → 8) incl. cart↔checkout↔products on 2 sites; holdout strict cross-site 1/11 → 2/11. 13 new tests (47 total).
- **AI-042-F1 — GOTO eval dimension + navigation corpus (2026-08-12)**: `docs/rag_corpus/playwright/04-navigation.md` (curated navigation reference — corpus previously had one navigation section; Playwright treats navigation as first-class), `scripts/eval/dataset/eval-008_goto_navigation.json` (GOTO-flavored banking story with `expected_page` from-contexts), flow path wired into `eval_resolver._resolve_placeholder` (GOTO/URL/url_assertion resolve via `flow_resolved_url`), 3 hermetic tests, stateful banking-mock scraped pages (login-wall + session-gated, localStorage auth). Banking resolver accuracy 9/26 → 13/26; overall resolver 32.1% → 35.8%; holdout eval 3/7 → 6/11; static gate unchanged 97.9%.
- **Cross-site flow memory — AI-042 session 2: eval holdout + route canonicalization (2026-08-12)**: `scripts/eval/flow_holdout_eval.py` measures flow transfer with holdout integrity (target site hash excluded) + from-context reachability. Finding: flows only transfer when sites share route vocabulary → `_ROUTE_ALIASES` in `normalize_route` (view_cart/basket/shopping_cart→cart, inventory→products, signin/sign-in/auth→login — exact whole-route match; step pages stay distinct). Result: **0 → 3/4 non-home URL asserts holdout-resolvable** (automationexercise products/cart via saucedemo+mock flows; ecommerce cart strict cross-site). Store re-seeded: 89 patterns / 5 cross-site.
- **Cross-site flow memory — AI-042 session 1 (2026-08-12)**: `src/flow_memory.py` — learns navigation *shape* from passing evidence (locators don't transfer across sites — only ~3% overlap — but login→browse→cart→checkout does). `normalize_route` (URL → route keyword, never raw URLs — AI-035 §4 privacy), `flow_transitions` (passed-only, same-page dropped, per-step site identity), `FlowMemoryStore` (JSON `evidence/flow_memory.json`, atomic writes, dedup + site diversity + `min_sites` cross-site guardrail), `flow_resolved_url` consumption hook wired as step 2.5 in the orchestrator's GOTO/URL-assertion chain (site-specific resolution always runs first). Learning wired into the conftest teardown hook + `synthesize_stories.py` parent sweep; `FLOW_MEMORY_ENABLED=0` hermetic gate in tests. 33 tests; seeded from 908 real sidecars → 64 patterns / 6 sites / 4 cross-site flows.
- **Output artifact validation — AI-043 Layer 3, live overlay alignment (2026-08-11)**: `src/heatmap_alignment.py` — renders the suite heatmap and asserts, in a real browser, that every overlay box centre hits the element it claims on the live page (locator-scoped `elementFromPoint` + ancestor/descendant containment in one evaluate; live document-size mapping catches wrong-frame drift when the page changed between steps; `missing`/`hidden` catches stale locators). `scripts/validate_report_artifacts.py --full` opens each recorded URL and runs Layer 3 (exit 1 on misalignment); offline mode unchanged. 18 offline unit tests + 3 live tests against the ecommerce mock (real chromium + real tracker metadata math; one shared browser per module to avoid Windows paging-file exhaustion under xdist). AI-043 marked complete on the roadmap. Also: doc audit — AI-034 stale checkboxes ticked, Phase 1 Multi-Agent checklist split into done vs NOT BUILT (dormant scope), BACKLOG Phase 4 Export TODO cleared.
- **Baseline comparison tool (2026-08-10, AI-041 support)**: `scripts/eval/compare_model_baselines.py` diffs two `eval_model_baseline.py` outputs in one command — auto-discovers `training_data/model_baseline_*.json` (older = before), per-story attribution via `story_head`, aggregate deltas (valid-skeleton rate, criteria cover, hallucinated login, skip lines, placeholders, errors), `--json` output, exit 0/2/1 (no-regression/regression/usage).
- **Output artifact validation — AI-043 Layers 1+2 (2026-08-10)**: `src/artifact_validation.py` — deterministic invariants for heatmap/Gantt/Plotly artifacts (heatmap points must be document-% in [0,100]; HTML payloads parseable/finite; aggregated counts consistent; Gantt durations finite ≥0; no NaN/None/empty series), `scripts/validate_report_artifacts.py` CLI (exit 1 on errors), golden fixtures in `fixtures/report_golden/` (good / legacy-pixel / NaN-duration), and 3 new Gate-0 checks in `scripts/smoke.py`. Running it over all 51 production evidence dirs caught real off-page markers (negative y).
- **Roadmap items AI-042/043/044 (2026-08-09/10)**: cross-site flow memory, output artifact quality gate, visual grounding — in `docs/plans/ROADMAP_ROADTO_PRODUCTION.md` Tier 3/4 + summary checklist.
- **Strix Halo 27B QLoRA field guide (2026-08-10)**: `docs/sessions/2026-08-10_strix_halo_27b_qlora_field_guide.md` — the working 4-bit training recipe on a 64 GB Windows Strix Halo box + a graveyard of 12+ dead ends (Studio's 4-bit→16-bit flip, BIOS modes, expandable_segments crash, batch/context OOM combos).

### Changed
- **B-047 residual: golden +20 bonus is now site-scoped (2026-08-10)**: `GoldenPattern.site_hash` seeded from dataset base_url (mock sites use canonical :8781/:8782/:8783 ports), stored in Milvus metadata, and enforced in both `_golden_pattern_bonus` and `scoring_bonus_for`. Also fixed a pre-existing round-trip bug — `site_hash` was missing from `MilvusLiteBackend.search` output_fields, which silently disabled the learned +5 bonus in production; real-Milvus round-trip regression tests added. Store rebuilt with site-scoped goldens (96 patterns, 7 distinct site hashes).
- **RAG learning lock fix (B-047 deferred, 2026-08-10)**: `learn_from_evidence_sidecars()` in `src/rag_learn.py` — the parent process sweeps `evidence/*.evidence.json` after each site's executions in `resolve_and_learn` (the pytest-subprocess hook is lock-blocked by the Milvus parent hold). Batch runs now actually learn (verified: 275 passing sidecars / 1120 learnable steps on disk).
- **Evidence coordinates are document-relative (2026-08-10)**: `_get_element_metadata` adds scroll offsets to the viewport-relative bbox before computing document-% `viewport_pct` (clamped to [0,100]) — fixes off-page heatmap markers for elements above the viewport (found by the AI-043 validator, e.g. y=-4.02 in production sidecars).
- **AI-041 training run: ❌ failed/closed (2026-08-11)**: Qwen3.6-27B trained successfully (4-bit QLoRA, loss 0.94→0.081, 15 steps/2h) but the GGUF export never completed — the 16-bit merge needs ~55 GB memory + ~110 GB disk peak that a 64 GB Windows box can't provide, and `merge_and_unload` doesn't exist on the Qwen3.5 arch. All artifacts deleted; runbook marked SUPERSEDED; see field guide.

### Added
- **Model-level baseline measurement (2026-08-09, AI-040/AI-041)**: `scripts/eval/eval_model_baseline.py` scores the live LLM's Phase-1 skeleton generation on the 35 synthetic stories through production's validation gates, capturing a full reproducibility envelope (model path, llama.cpp /props: n_ctx/temp, git commit + dirty flag). Baseline captured for `Qwen3.6-27B-UD-Q4_K_XL.gguf`: valid skeleton 100%, criteria cover 100%, hallucinated login 0%, 1504 placeholders; pipeline eval static 97.9%. After fine-tuning, re-run the same script for a trustworthy before/after.
- **Training runbook + backlog (2026-08-09, AI-041)**: `docs/sessions/2026-08-09_unsloth_training_runbook.md` — AMD/Strix-Halo capability summary, model selection (safetensors only; NVFP4 is NVIDIA-only, 27B bnb-4bit doesn't exist yet), Studio settings table, GGUF export + model-swap flow (no .env edit — pipeline auto-detects via /v1/models), and baseline comparison steps. BACKLOG tracks the training run as AI-041.
- **`--resolve-and-learn` full combo (2026-08-08, AI-040)**: `synthesize_stories.py` Stage 4 resolves pre-generated skeletons against live/mock sites (production `TestOrchestrator.run_pipeline`), executes them, and auto-learns passing steps into the RAG store via the conftest hook. `--rag-both` runs RAG-assisted + cold-start passes (2× resolution data); append-mode dedupe persists story → resolved-code rows into `training_data/playwright_resolved_alpaca.jsonl` (112 rows across all 7 sites, 3464 resolved evidence calls). Mock sites auto-start per-site servers on :8781-8783 (repo-root for lv_insurance, `mock_sites/<site>` for banking/ecommerce). Full combo run: mocks × RAG on+off 56 passed/6 failed, live × RAG on 20 passed/23 failed.
- **Fine-tuning dataset generation tooling (2026-08-07, AI-040)**: `scripts/build_finetune_dataset.py` converts pipeline artifacts (`scrape_manifest.json`, eval golden keys) into Alpaca-format JSONL for Unsloth Studio — (story → skeleton) rows plus (placeholder → locator) resolution rows; `scripts/synthesize_stories.py` LLM-synthesizes new stories per eval site (anchored to a real element inventory), runs the offline Phase-1 skeleton generator through production's validation gates, and merges passing rows (`--mode linear|graph|both`; graph is deterministic temp=0, linear is stochastic). Seed corpus in `training_data/`: 172 skeleton rows + 90 resolution rows. Also surfaced a llama-server config issue (156K context → Vulkan OOM on the 48 GB Strix Halo UMA; relaunched at 9072).
- **Test-pack split: contract/adversarial/resilience layers** (2026-08-07, work item 2): `tests/contract/` (6 tests — mock artifact/schema/import/route/behaviour contracts), `tests/adversarial/` (7 tests — 404-page pollution, overlay injection, broken-locator, modal scoping), `tests/resilience/` (6 tests — corrupt DB, reload-safe RunResult, sidecar-without-teardown, concurrent opens, corrupt settings). Default pytest runs them offline via `-m "not slow and not integration"`; they run in CI.
- **`scripts/gate_full.py`** (2026-08-07, work item 3): one-command verification chain smoke → unit pytest → eval-static → verify_production → export_gate, exit non-zero on first failure; `--offline` (gates 1-3, CI-able), `--skip N`, `--pytest-args`.
- **Banking mock site + eval-007** (2026-08-07): `mock_sites/banking/` — 7-page static banking target (sign-in → dashboard → account detail → transfer → pay bills → success pages), localStorage session/balances, injectable consent/ad overlay, route aliases. Eval dataset eval-007 (8 criteria, 13 golden placeholders): **static 13/13 (100%), execution 8/8 passed**. Priority-2 mock in the catalog; the first auth-gated mock.
- **Run & Fix page** (2026-08-06): the app is now a 3-page workflow — Test Generator (setup/generate only), Run & Fix (generated code → run → repair → evidence → export), Evidence & Reports (history + compare). Results/repair/evidence/export moved off the generator page; empty-state with handoff; sidebar-loaded packages surface immediately (hydration) and auto-restore across sessions (`SETTING_LAST_PACKAGE`).
- **Package dropdown labels** (2026-08-06): the Load-package selector now shows readable date · site · story snippet · run counts instead of raw `test_YYYYMMDD_<slug>` names.
- **Run comparison** (`src/ui/ui_run_comparison.py`, 2026-08-06): pick a package + two runs on Evidence & Reports → per-test delta table + Changed/Fixed/Regressed metrics (filtered per-package — `load_all_run_results` ignores its directory arg).
- **Self-heal honesty** (2026-08-06): a no-results test run (timeout/collection) surfaces `run_error` instead of "✅ All tests pass"; LLM-reviewer failures name the affected tests instead of silently counting them unfixable; `heal()` resolves package-directory `saved_path` to the test file; pytest timeout raised 300s → 600s (`PIPELINE_TEST_TIMEOUT`, matching the app's runner).
- **Re-run Failed merge** (2026-08-06): `merge_rerun_results()` keeps passing tests in the table after a failed-only rerun (previously they were dropped).

### Fixed
- **Skeleton prompt taught login hallucination (2026-08-09, root cause of 55 bad rows)**: the Phase-1 prompt's EXAMPLE OUTPUT showed `{{FILL:username:admin}}` / `{{CLICK:submit button}}` unconditionally, so the LLM invented login steps for guest-flow sites (ecommerce/lv_insurance/demoqa). Example now shows a guest cart flow + a DO NOT INVENT AUTH rule (only emit auth steps when criteria reference them). Both byte-identical templates updated (`src/prompt_builder.py` t-string, `src/prompt_utils.py` legacy).
- **`login` keyword resolved to seed URL instead of the login page (2026-08-09)**: `UrlResolver.build_mapping` hard-pre-mapped `login` → seed, so `{{GOTO:login}}` landed on the homepage and fills like `username` failed (their DOM lives on `/login`), skip-marking whole tests even though locators had resolved. `login` now flows through normal keyword matching (scraped `/login` wins; SPA sites fall back to seed). Regression tests added.
- **Training-data poison: hallucinated-login rows (2026-08-09)**: `build_finetune_dataset.py --clean` drops rows containing login steps (`standard_user`/`secret_sauce`/`{{FILL:username}}`) on login-less sites. 55 rows removed across skeleton/resolved/synthetic sets; ecommerce skeletons regenerated with the fixed prompt (7 clean rows appended). Corpus now 0 hallucinated-login rows.
- **MockServer cwd pollution (B-039 note)** (2026-08-07): `MockServer._start()` did `os.chdir(served_dir)` and never restored it, breaking relative paths for any caller after auto-start (eval `--dataset <relative>`, a second server start in one process). The server now serves via the handler's `directory` kwarg — the caller's cwd is never mutated.
- **Banking-mock pipeline gaps (B-045)** (2026-08-07): five site-agnostic resolver/executor bugs the new mock made deterministic — (1) HTTP-404 pages surviving `_drop_dead_pages` (stdlib error body scrapes to 5 elements, above the 3-element dead-shell threshold; its "Error code: 404" text won ASSERT matching) now dropped via a content-based `_is_error_page()`; (2) login-click transition vocabulary was ecommerce-only (`inventory`/`products`) so auth-gated journeys never left the sign-in page — extended to `dashboard/accounts/home/overview`; (3) submit-success page transitions missing (form submits have no href, so success-message asserts resolved against the form's own submit button/error paragraph) — added transfer/payment submit→success inference; (4) role-worded descriptions ("pay bill button") fast-matched header nav links in Pass 1/2 before scoring — new `_named_role_in_description()` role gate + exact-text pre-sweep + submit-intent bonus + fillable-element CLICK penalty; (5) `fill()` crashed on native `<select>` (Playwright rejects it) and the LLM's fill value rarely equals the option `value` — `EvidenceTracker.fill()` probes the tag and routes to `select_option()` with value→label→substring fallbacks. Also: golden validator `_normalize_locator()` strips leading tags from class selectors (`p.account_balance` ≡ `.account_balance`), lifting eval-006 12/16 → 14/16 and eval-007 to 13/13. Eval static overall 95.2% → **97.9%**.
- **Locator-repair dedent crash (B-042)** (2026-08-07): a "🔧 Fix Locator" patch could silently break the whole suite at COLLECTION time — `apply_patch` rebuilt the patched line from regex groups that excluded the leading indentation, so a repaired line inside a test function landed at module scope (`NameError: evidence_tracker` → 1 error, 0 tests). The reconstruction now re-applies the original line's indent; an empty `original_locator` (which previously matched every line and mangled the file via `str.replace`) now raises `LocatorRepairError` instead.
- **Sidebar/CLI package dropdown "0 runs" (B-043)** (2026-08-07): package run counts came from manifest fields only bumped by CLI runs, so evidence-bearing UI runs (persisted to the workspace SQLite DB) never showed — a package with 13 real runs displayed `(1 test, 0 runs)`. `find_existing_packages` now reconciles against real run history via the new `run_stats_by_package()` (`GROUP BY test_package`), with legacy per-package counting and manifest values as fallbacks.
- **Run results silently vanishing (B-044)** (2026-08-06): Streamlit's module watcher reloads `src` modules mid-session, creating a NEW `RunResult` class — stored instances then failed `isinstance`, hiding results/evidence (this caused the recurring "results disappeared" reports, misattributed to UI toggles). New `is_run_result()` duck-type check (`TypeGuard`) replaces all 12 `isinstance(RunResult)` gates.
- **Reports showed 0.00s durations** (2026-08-06): sidecars never wrote a per-test duration; `evidence_serializer` + `evidence_tracker` now record `duration_s` (real wall-clock).
- **Reports showed "6. 6. [T06]" titles** (2026-08-06): the criterion's own enumeration was duplicated by the report's index; `report_builder` strips the leading "N. " prefix.
- **Reports written to the wrong directory** (2026-08-06): `build_report_bundle` did `Path(saved_path).parent`, mis-resolving when `saved_path` is a package directory (sidebar loads) — reports landed in `generated_tests/` instead of the package.
- **POM double blocks** (2026-08-06): the LLM skeleton emitted duplicated page-object imports/instantiations (`home_page` ×3 per test); `deduplicate_pom_lines()` (wired into the orchestrator) keeps one per page object.
- **Evidence screenshots captured mid-load** (2026-08-06): product grids showed blank images; `_record_step` now waits (bounded, 4s cap) for in-flight images before capturing, plus a 500ms post-navigate settle.
- **Click steps had no evidence** (2026-08-06): clicks only screenshotted on failure; success captures added (direct, hover-fallback, and scoring-fallback paths).

### Changed
- **3-page navigation** (2026-08-06): Test Generator / Run & Fix / Evidence & Reports (was 2 pages with results mixed into the generator).
- **Export panel** moved to Run & Fix (2026-08-06) so you export the package you can see.
- **HTML report embeds failed-step screenshots** (2026-08-06) — base64, self-contained; passing galleries stay on the Evidence page.
- **Living Test Plan UX** (2026-08-06): `Reviewed` moved to the far right (away from the headerless delete checkboxes), delete-row caption, flagged-condition warning + tooltip.
- **This-run evidence gating** (2026-08-06): stale sidecars from previous sessions no longer appear as "this run" — evidence renders only after a real session run.
- **`SETTING_MODEL_NAME`** now used by `streamlit_app.py` (2026-08-06) — the persistence existed via a literal key; the dead constant is wired in for clarity.
- **B-039 — Self-healing unblocked on real failures** (`src/pytest_output_parser.py`, `src/failure_classifier.py`, discovered live during AI-035 Tier-1 verification): (1) the failures-block name regex rejected `[chromium]`-suffixed headers, so `error_message` was always empty for parameterized tests — self-healing classified every real failure as OTHER and pre-screened it as unfixable; the header regex now tolerates param suffixes and strips them before the lookup. (2) `failure_classifier` now recognizes the evidence-tracker fast-fail (`_LocatorNotFoundError: Locator '...' not found on current page`) as `LOCATOR_TIMEOUT` with locator extraction, so the LLM reviewer actually sees the product's most common failure mode. Verified live: broken locator → self-heal → `fixed: 1, learned: 1, remaining: 0`, store gains `CLICK 'Cart link' → a[href="/cart.html"]` with `source=self_healing, confidence=1.0`, re-heal dedups (hit_count bump, one row).
- **B-036 Phase 4 Tier-2 walkthrough** (`streamlit_app.py`): the settings-persistence acceptance was verified live against the real Streamlit app (Playwright) — POM mode, provider, consent mode, OCR backend and workspace all round-trip across app restarts via `~/.ai-test-gen/settings.enc`, and the RAG Store panel shows the learned-pattern stats. The walkthrough also closed the last migration gap: the Streamlit UI now persists `provider_base_url` + `model_name` (save-on-change + seed-on-load), completing the "provider/model selection" migration from the spec.
- **AI-035 self-healing write-back** (`src/rag_learn.py` + `src/self_healing.py`): when the self-healing loop applies a verified `replace_locator` patch, the corrected `(description, locator)` pair is written back to the RAG store — `pattern_from_patch` maps the patched code line to a `LearnedPattern` (`confidence=1.0`, `source="self_healing"`), recovering the placeholder description from the failing test's evidence sidecar (matching the failed selector to its step label, `{{CLICK:view cart link}}` → `view cart link`), scoped by `site_hash(domain)`. `learn_from_patch` is guarded (never breaks healing); `HealingReport.learned` + CLI/UI display the count. This closes AI-035's original trigger — the self-healing lever and the learning loop are fully wired. +27 tests (2256 total).
- **B-036 Phase 4 — Settings store + field migration** (`src/settings_store.py`): consumer-grade settings that survive restarts, on the `secure_config` pattern — Fernet-encrypted `~/.ai-test-gen/settings.enc` (machine-keyed, corruption-tolerant, never crashes a run; separate file from `config.enc` so key storage and settings storage can't clobber each other). API: `SettingsStore` class + module-level `load_setting/save_setting/save_settings/get_all_settings/reset_settings`. Migrated sidebar state consumers actually set: `pom_mode`, `consent_mode`, `provider`/`model_name`, `workspace` — Streamlit sidebar (`SidebarConfig.render()`) and CLI `Session` (`create_session()` seeds from the store; settings win, env is fallback). `JIRA_PROJECT_KEY` env read removed from `src/config.py` (constant default `TEST`) — it's now an **export-time field**: Streamlit export panel + CLI menu (`Session.jira_project_key`), feeds `JiraReportGenerator` test-case IDs and a `Project:` header line in the Jira report (`PipelineReportService.build_reports(jira_project_key=...)`). `OCR_BACKEND` → persisted setting (default `pymupdf`); env read in `get_ocr_backend()` is now a fallback only. `LANGGRAPH_ENABLED` removed outright (dead flag — `--use-graph` is the supported path; `generate_skeleton(use_graph=...)` replaces the env read; orchestrator/eval-runner env reads deleted). Streamlit "Learned Patterns" settings section folded in (`SidebarConfig.render_settings()` — RAG store stats via `store_stats()` + guarded prune button). +30 tests (2229 total); eval static 95.2% unchanged.
- **B-036 Phase 3 — Evidence auto-learn (AI-035 core)** (`src/rag_learn.py`): when a generated test step passes, the verified `(action, description, locator, site)` pair is written back to the local RAG store — `RAGStore.upsert_pattern()` dedups on `(action_type, description, site_hash)` (repeat bumps `hit_count`, store stays bounded); teardown hook in `generated_tests/conftest.py` learns from passing runs (guarded, batched, never breaks a run). Site-scoped scoring: `SAME_SITE_LEARNED_BONUS = 5` for same-site learned patterns, **0** for cross-site (anti-poisoning), threaded orchestrator → matcher → resolver → scorer. Privacy: only one-way `sha256(domain)` stored — no URLs/PII. Plan: `docs/plans/AI-035_B036_P3_plan.md`. Verified live against the e-commerce mock (dedup'd learned patterns with hit counts); eval static 95.2% unchanged.
- **B-036 Phase 1 — Always-on RAG** (`src/orchestrator.py`, `src/rag_retriever.py`): the retriever now builds by default — no `RAG_ENABLED=1` needed; `RAG_ENABLED=0` remains a transitional opt-out. Graceful degradation keeps behavior identical to the pre-RAG pipeline: empty store ⇒ no patterns ⇒ no bonus; any store/embedder failure (offline model download, corrupt DB) returns no patterns instead of raising, so RAG can never block generation (warning logged once per retriever). Unit tests made hermetic: the default suite forces `RAG_ENABLED=0` so machine-local store state can't change test outcomes; resolution accuracy is exercised by the eval harness.
- **B-036 Phase 2 — Bundled golden pack + auto-seed** (`src/rag_bundled.py`): ships the eval golden keys (`scripts/eval/dataset/eval-001..006`, 83 patterns incl. both mocks) + curated Playwright docs (27 chunks). First generation run seeds the store automatically with an idempotent marker (`evidence/.rag_bundled_seeded.json`, versioned); re-runs are a no-op. `rag_ingest.py --bundled` (re-seed, `--force` to force), `--stats` (per-type counts), `--prune-learned` (drop learned patterns, keep golden/docs — active once learning lands). `RAGStore` gains `counts_by_type()` / `delete_learned()`.
- **E-commerce mock site** (`mock_sites/ecommerce/`): deterministic, offline, localhost test target mirroring the automationexercise DOM classes the pipeline already knows (`#cartModal`, `.btn-success.close-modal`, `a.check_out`, `.add-to-cart`, `#empty_cart`). 6 static pages (home → products → product details → cart → checkout → success), localStorage cart, offline SVG product images, and an **injectable consent/ad overlay** (`?overlay=consent|ad`) that reproduces the B-029 click-swallow race on demand. **Route aliases** (`mock_routes.json` + `scripts/mock_server.py` 302-redirects) map the pipeline's keyword vocabulary (`/view_cart`, `/products`, `/checkout`, `/basket`…) to canonical files so journey discovery and cart-seeding reach cart/checkout *with items*, and page URLs stay canonical for `to_have_url`. Eval dataset **eval-006** (8 criteria, 16 golden placeholders, full checkout/payment leg — the eval-002 gap) + committed capture. Measured: execution **8/8 passed** against the mock.
- **FEATURE_SPEC_B036 consumer config** (`docs/specs/FEATURE_SPEC_B036_consumer_config.md`): always-on RAG (degradation already built), bundled golden pack auto-seed, evidence auto-learn (builds on AI-035 — the separate `learned_patterns` SQLite table is explicitly superseded), settings store on the `secure_config` pattern, Jira key → export-time field, `LANGGRAPH_ENABLED` removal. ~3 sessions.
- **Roadmap Tier 6 — Product Expansion** (FC-02 API, FC-03 .NET, FC-04 dashboard testing): beyond-browser-E2E directions on the kanban (23 roadmap items), with the guardrail that discover/resolve/emit/execute stay swappable seams.
- **Roadmap Tier 7 — User Documentation & Onboarding** (UD-01 user guide, UD-02 option-explanation UI revisit): records that the product ships with zero user-facing docs, inventories the Streamlit-vs-CLI option asymmetry (found in the B-036 Tier-2 walkthrough — e.g. OCR backend and workspace are Streamlit-only, the CLI reads them from the store but can't set them from the menu), and is **explicitly deferred** until the paid/free tier split (Phase 6/8) so docs aren't written twice. Now 24 roadmap items on the kanban.
- **Offline-suite guard** (`tests/test_no_live_network_in_default_suite.py`): static AST guard that fails if any unmarked test executes a navigation call (`goto`/`navigate`/`scrape_url`/`scrape_all`/`run_pipeline`/`attempt_login`) with a live-site URL literal — makes the `-m "not slow and not integration"` contract (default pytest is deterministic and offline) durable against new code. Skips tests that explicitly mock the network layer. Verified: planted live-goto/scrape/pipeline offenders are flagged; mocked + config-assertion tests are not.
- **Eval static in CI** (`.github/workflows/ci.yml`): new `eval-static` job runs `eval_harness.py run --mode static --min-accuracy 79` (offline, ~0.5s, exit 2 below floor) in parallel with lint/type-check — free resolver/scorer/prompt regression protection on every push.
- **Export gate** (`scripts/export_gate.py`): the export analogue of `verify_production.py` — exports a package in both flat + POM modes, validates artifacts (no evidence remnants, no stub bodies, POM pages shipped, run-history DB copied), collects both suites, then runs them and asserts pass. Default source is a bundled golden fixture (`fixtures/golden_package/`) mirroring a real generated package (arg-carrying evidence decorators, POM pages, run-history DB) that targets a tiny localhost site — deterministic, no external network, CI-able. `--source <pkg>` validates a real package; `--run-remote` adds live execution. Exit 0/1.
- **Golden export fixture** (`fixtures/golden_package/` + `fixtures/golden_site/`): minimal POM-mode package (evidence-aware POMs, `@pytest.mark.evidence(condition_ref=..., story_ref=...)` decorators, `evidence/run_results.sqlite`) targeting a 2-page static site served by the gate on `127.0.0.1:8123`.
- **Step-through test debugger** (`scripts/debug_step_through.py`): runs real generated test functions in a headed Chromium window, pausing after every tracker step and printing the live overlay/modal state the auto-dismissal logic hides (add-to-cart modal, FreeCmp consent dialog, Google vignette, cart-link count, URL). `--auto`/`--headless` for non-interactive runs. Surfaces the invisible `EvidenceTracker.click()` dismissals.
- **Mock-site catalog** (`mock_sites/README.md`): plan for a local catalog of deterministic test targets across 8 product types (e-commerce, banking, insurance, booking, healthcare, HR, widgets, security) — no ad/consent overlay noise, CI-capable, golden keys never decay. E-commerce mock is build priority #1 (exercises the B-029/B-030 bug classes).
- **BACKLOG audit (2026-08-03 CLI review)**: logged B-029→B-036 — false-pass clicks w/o navigation verification, `#do_action` wrapper-div resolution, broken export suites, AI-012 orphaned DB copy, evidence gaps (no failure screenshots), corrupted `evidence/run_results.sqlite`, end-only sidecar persistence, consumer config architecture (env-var gates → always-on/UI). Plus test-pack restructure strategy (contract/adversarial/resilience layers, eval static in CI) and the mock-site catalog.

### Fixed
- **E-commerce mock resolver gaps (B-037)**: `#empty_cart` was winning "product name and price" (empty-state text now excluded from content ASSERTs via `_assert_empty_state_rejects` — the B-016 negation gate only ran in pass-1, not scoring); the "cvc" FILL was unresolvable against the "CVV" field and skipped the whole checkout test (cvc↔cvv↔cvv2 synonyms added); "card number" filled the Cardholder Name field (`name="card_name"` shared the word "card" and won the pass-1 tie — mock now uses `cardholder_name`, and CSS classes participate in `_structural_bonus` so `.cart_total_price` matches "price"). Eval-006 execution: **6/8 → 8/8 passed** (full checkout + payment leg executes). +9 regression tests. Remaining static misses are LLM skeleton/ranking nondeterminism (the AI-037 class), now isolated from site variance by the deterministic mock.
- **Golden fixture run-history DB schema** (`fixtures/golden_package/evidence/run_results.sqlite`): regenerated to exactly mirror the real `SQLitePersistence` schema (`runs` + `test_results` + indexes, verified column-by-column) — the export gate copies this DB, and a consumer opening it with the real app previously hit a made-up schema.
- **Exports are now runnable and validated (B-031)**: POM-mode export globbed `pages/po_*.py` but generated pages are `home_page.py`/`cart_page.py` — glob now matches all `*.py` (minus `__init__.py`) so the pages that tests import are shipped. POM-mode export now preserves POM structure (imports, instantiations, method calls) instead of silently producing flat output with a dead `pages/` dir (`strip_evidence_from_test_code(..., preserve_pom_calls=True)`). `@pytest.mark.evidence(condition_ref=..., story_ref=...)` decorators are stripped in all forms (bare, arg-carrying, multi-line, whitespace variants) — previously only the bare form was removed, leaking `PytestUnknownMarkWarning` into exports. The B-020 assert family (`assert_hidden`, `assert_disabled`, `assert_enabled`, `assert_checked`, `assert_empty`, `assert_text`, `assert_text_contains`, `assert_value`, `assert_count`) is now converted to Playwright `expect()` in both test code and POMs — `assert_hidden` was the live gap (survived exports → runtime `NameError`). Exporting an all-stub / all-skip / test-less source package now raises `ValueError` instead of silently producing a non-runnable suite (34 of 35 historical exports were stubs). Same-second same-slug exports no longer overwrite each other (unique `_1`, `_2`… suffixes).
- **Export run-history copy fixed (B-032)**: export copied `evidence/playwright_tests.db` — a name nothing in the repo creates — so the run-history copy was a silent no-op since AI-012. It now copies `evidence/run_results.sqlite` (legacy `playwright_tests.db` fallback for old packages; WAL/SHM follow the found name). Same fix in `pipeline_artifact_manager._count_run_results` (also fixed a Python-2 `except A, B:` that worked by accident).

### Fixed
- **False-pass clicks now fail truthfully (B-029)** (`src/evidence_tracker.py`): a "successful" click on a link whose URL never changes is now detected — overlays are dismissed and the click retried once, then the recorded step is amended from `passed` to `failed` instead of silently continuing on the wrong page (the cause of the checkout-cluster failures). Unscoped `button.btn-success.close-modal` dismissal scoped to modal containers.
- **Failed steps always carry evidence (B-033)**: fast-fail steps (locator missing/hidden) now capture a screenshot + failure note (previously skipped), every step records its `url`, and screenshot-capture failures log a warning instead of being silently swallowed. The enshrined test asserting `screenshot is None` on fast-fail was flipped to the new contract (+3 new B-029 contract tests).
- **Evidence sidecar persists incrementally (B-035)**: `_record_step` writes the sidecar on the first step and on any failed/partial step, so a killed/timed-out test leaves real evidence instead of orphaned PNGs.
- **Corrupt evidence database self-heals (B-034)**: `SQLitePersistence` recreates a corrupt database file at construction; `EvidenceIndex` recovers + retries on `DatabaseError` (never drops a healthy DB), and the live `evidence/run_results.sqlite` was healed + rebuilt (359 sidecars indexed). Also fixed the Python-2 `except A, B:` syntax in `evidence_index.py` (worked by accident). +2 corruption-resilience tests.
- **"Check Out" no longer resolves to a wrapper div (B-030)** (`src/placeholder_scorers.py`): the B-025 "clickable container" bonus (+10) outranked the real button's interactive bonus (+5), so `{{CLICK:Check Out}}` emitted `#do_action` (a div whose click does nothing) over `.btn.btn-default.check_out`. Container bonus now +3 — below link/button (+3 role +2 href) — so interactive elements win when both match; containers still win text-only matches. Eval static 100% (no regression) + 2 regression tests.

---

### Added
- **Soft-404 SPA recovery** (`src/scraper.py`): saucedemo (SPA on GitHub Pages) serves every `.html` path as HTTP 404 from an app shell that JS-redirects to the real view. `_scrape_url_sync_result` now proceeds when the final URL differs from the requested one (`_is_soft_404`), instead of bailing with `HTTP 404` and zero elements.
- **Site-agnostic stateful routing** (`src/url_utils.py`): `is_stateful_cart_checkout_path()` replaces the automationexercise-hardcoded `{/view_cart, /checkout}` path set — covers saucedemo's `/cart.html`, `/checkout-step-one.html` etc.
- **Concept-driven URL candidates re-enabled** (`build_common_path_candidates`): same-domain candidates from the shared route vocabulary (mirrors `journey_scraper.keyword_routes`); SPA sites have no hrefs for journey discovery, so cart/checkout URLs previously never entered the scrape set.
- **Dead-page + redirect-duplicate filters** (`src/placeholder_orchestrator.py`): <3-element SPA 404/login-wall shells and pages whose stateless scrape redirected to another page with duplicated content are dropped before resolution.
- **Navigation-intent fallback**: cart/basket navigation descriptions ("cart icon", "shopping cart") that fail element matching (SPA icons have no accessible name) re-resolve as GOTO to the verified page URL, keeping page context advancing through cart → checkout.
- **Post-login ASSERT mapping**: "logged in"/"login successful" assertions resolve to the inventory/products page, not the login page.
- **Semantic alias matching** (`src/url_resolver.py`): generic keyword→route aliases (products → `/inventory.html`, cart → `/basket`, login → `/signin`) — no per-site lists.
- **B-024g field matching** (`src/element_matcher.py`): separator-normalized word-subset fallback for FILL — "zip code" matches placeholder "Zip/Postal Code"; `normalise_element_text` now includes placeholder as last-resort text source.
- **`verify_production` saucedemo credentials**: env-overridable `SAUCEDEMO_USERNAME`/`SAUCEDEMO_PASSWORD` (default `standard_user`/`secret_sauce`, mirroring eval) passed to `TestOrchestrator`.
- **Modal-close no-op** (`src/evidence_tracker.py`): clicking a modal-close control whose modal is already dismissed records a satisfied no-op instead of failing (generated "close popup" steps collide with the tracker's pre-click auto-dismiss).

### Fixed
- **Journey subprocess dropped credentials**: `run_journey_subprocess_entry` serialized `credential_profile` in the payload but never read it back — auth-gated journeys silently ran without a session (saucedemo hit the login wall). Profile is now reconstructed and passed to `JourneyScraper`.
- **Journey scraper never logged in**: `JourneyScraper` now calls `attempt_login` at the starting URL when a `CredentialProfile` is present.
- **B-015 ghost (3 places)**: `_dismiss_modals`, `_dismiss_confirmation_modals`, and the repair setup script clicked `button:has-text("Continue Shopping")` globally — saucedemo's cart-page button navigated journeys and generated tests back to inventory, so `cart.html` was never captured and `#checkout` never clicked. Text-based dismissal is now scoped to modal/dialog containers.
- **Dead candidate URLs won ASSERT resolution**: guessed `/basket`, `/checkout`, `/inventory.html` keys (2-element shells or redirect-to-home duplicates) out-ranked real pages — dropped by the new filters; GOTO resolution now searches all verified pages, not the scoped current page.
### Added
- **Eval harness full-regenerate test execution**: `EvalRunner._persist_regenerated_tests()` writes just-generated code to `generated_tests/test_<site>.py` so `--mode full --regenerate` actually runs the tests — "Tests executed: 0" (every run) is now 33 executed / 17 passed (51.5% pass rate) with per-site breakdown and false-positive estimation.
- **Dialog-action scoping (Pass D)**: `ElementMatcher.pass_dialog_action()` — descriptions implying dialog/dismiss/confirm intent (ok/okay/close/dismiss/confirm/cancel/accept/done/continue/got it) resolve against in-modal interactive elements (`in_modal` flag / dialog role), preferring the modal's dismissal control (close-modal class semantics). Generic ARIA-based — no site-specific lists. Plus CLICK fast-path hygiene (`_hidden_element_penalty` + `_click_text_penalty` in the haystack fast path) and pass-2 hidden/role gates + ≥3-char substring containment ("OK" can no longer match "csrfmiddlewareTOKen").
- **Assertion-state polarity**: `polarity_assertion_type()` (closed/gone/disappeared/removed/hidden/dismissed/vanished/no longer/not visible/not shown) → `toBeHidden` → `EvidenceTracker.assert_hidden()` (`wait_for(state="hidden")` — hidden OR detached). Hooked at both ASSERT resolution paths.
- **`Session.story_slug`** property (slugify of the pasted story) — fixes the export flow's AttributeError.
- **CLI walkthrough `reject:` capability** — a step now fails when rejected text (e.g. "Export failed") appears in output, catching errors the loose "Press Enter" markers previously swallowed.

### Fixed
- **Page-load assertions**: `_is_page_state_assertion` no longer vetoes "title" — `<page> page title` routes to `to_have_url` (matches the golden encoding; "practice form page title" stays element-level). `resolve_url` root-path substring bug (a bare root URL no longer matches every multi-word description, e.g. "cart page loaded" → home URL). Golden validator compares `to_have_url` URLs trailing-slash-insensitively (production emits `https://host/`, goldens hold the bare form).
- **CLI tables truncated at 50 chars**: Living Test Plan and Test Table now wrap text to the full terminal width (`shutil.get_terminal_size` + `textwrap`) with continuation lines.
- **CLI log/menu interleaving**: `LLMClient._debug` and `TestOrchestrator._debug` (plus the short-response warnings) now print to stderr — `[llm_client]`/`[pipeline]` lines no longer pollute menu/table output under `PIPELINE_DEBUG=1`.
- **Export Clean Package produced empty suites**: `pipeline_saved_path` holds the test-FILE path (ui_pipeline) but export treated it as a directory — file→parent-dir normalization; export now carries the tests ("Tests: 1").
- **Flat export left broken POM references**: `strip_evidence_from_test_code` now removes POM imports/instantiations and converts `_page.click/fill(label, selector='sel')` → `page.locator('sel').click()/fill(...)`; `expect` import is idempotent (no more `import Page, expect, expect`). Exported flat tests are valid, runnable Playwright.
- **CLI walkthrough heal-flow staleness**: step markers updated for the "2 test(s) still failing. → Choice:" outcome (was a 1200s timeout).

### Changed
- **Skeleton prompts (all four templates)**: page-state ASSERT form added — "For 'verify <page> loads/opens' use `{{ASSERT:<page> loaded}}` (→ URL check). Do NOT write `{{ASSERT:<page> title}}`." Plus a polarity rule: "For disappearance checks ('popup closed', 'item removed') describe the ABSENCE." Legacy `prompt_utils.py` templates kept byte-identical to the t-string versions.

### CLI walkthrough driver
- **CLI walkthrough driver** (`scripts/cli_walkthrough.py`): marker-driven subprocess driver that exercises every CLI menu button — `--pass nav` (41/41, ~1 min) and `--pass full` (60/60 with real LLM + live site, ~15 min). Documents Windows pipe gotchas (use `read1()`, one write per `input()`, paste terminates on first blank line). Full logs to `scripts/archive/cli_snapshots/`.

### Fixed
- **CLI "Load Existing Generated Tests" crash** (`PermissionError`): `load_package_manifest()` was handed a package directory instead of the manifest file; now resolves `<dir>/package_manifest.json` or reconstructs, and both CLI callers pass `reconstruct=True` (legacy/`verify_*` packages have no manifest). 4 new tests.
- **CLI POM Mode / Consent Mode invisible toggles**: State block now shows `Consent : …` / `POM Mode : ON/OFF`; toggle handlers pause with a visible confirmation (also fixes a stray-Enter msvcrt bug that re-selected menu item 1).
- **POM mode dropped resolved selectors** → generated tests skipped at runtime: `home_page.click('product name link')` relied on a fuzzy runtime matcher that requires 2-word `_ELEMENTS` overlap. `get_pom_method_call` now emits `click(label, selector='…')`/`fill(..., selector='…')` and generated POMs click the resolution-phase selector directly (runtime matching is the fallback). Generic `fill()` added to generated POMs (previously absent → would skip).
- **Consent-overlay pollution**: OneTrust class-based markup (`.fc-*`, `#onetrust-*`) stayed in the DOM hidden and drowned scrapes (1,448/2,328 elements on automationexercise); consent removal matched only ID-based selectors. Added class-based selectors — POMs shrank 1806 → ~520 lines.
- **URL trailing-slash mismatch**: `expect(page).to_have_url("https://host")` failed after sites canonicalize to `host/`. `normalize_url()` applied at every URL emission point (GOTO steps 1-4, both ASSERT batch paths).
- **FILL resolved to a container div** (saucedemo `[data-test="login-container"]` reports accessible_name 'Username' by wrapping the input): fillability gate added to `pass1_text_match` (already present in `rank_candidates`).
- **Evidence-tracker post-navigation hang**: after a click that navigates, `_record_step` re-queried the now-dead locator's metadata — each un-timed Playwright call waited the 30s default (≈120s/test), blowing suite timeouts. `_record_step` accepts pre-captured `element_metadata=`; suites complete in ~140-175s.
- **verify_production timeout message + salvage**: printed the literal expression `{max(60, min(180, len(test_funcs) * 25))}s`; now shows the real value and saves partial pytest output + evidence count on timeout (`timed out after 180s — 1/6 tests completed`). Suite cap raised to `min(300, tests*30)`.
- **AI-037 Phase 3 — skeleton journey-structure guidance**: JOURNEY STRUCTURE rules added to both skeleton prompts (`build_skeleton_prompt` + `build_single_condition_prompt`, t-string and legacy byte-identical) — fill all fields on the current page before navigating; never place a step after the navigation that leaves its page; no `pytest.skip` for unplaced steps; use exact story labels. `JourneyScraper._scrape_current_page` now reveals SPA hidden sections before capture (mirrors the frozen-capture methodology — fixes hidden-section CLICK/FILL targets being hard-skipped as `is_visible=False`). Golden validator `_locators_match` now treats `:has-text()` needles as substring-equivalent (Playwright semantics). LV regeneration 15/24 → **19/24 (79.2%)**; ideal-skeleton live pipeline 21/24 → **24/24**; static eval 100% all sites.
- **AI-037 resolver fixes**: radio/checkbox label capture (scraper), clickable-div-with-id capture, `<strong>` display capture, synthetic-ARIA marker, radio `input[name][value]` locator format, quote-agnostic locator normalisation, camelCase splitting in `SemanticMatcher.get_words()`, Pass 1 synthetic-container skip, proportional text-content bonus + punctuation normalisation. LV Insurance resolver 23/24 → **24/24 (100%)**; regeneration 54% → **62.5%**. 15 new tests. `scripts/eval/refresh_lv_capture.py` reproduces the frozen LV eval data in journey state.
- **PEP 750 t-string PromptBuilder**: `src/prompt_builder.py` — structured prompt assembly that keeps trusted static structure separate from untrusted interpolated values. Per-field transforms (truncation) keyed by interpolation expression; `RenderedPrompt.to_log_entry()` emits a JSON-serialisable audit record (fields, truncation, static-vs-dynamic split). Templates: `build_skeleton_prompt()`, `build_single_condition_prompt()`. 13 tests.
- **Phase 1 Multi-Agent Architecture (LangGraph)**: Complete document-driven pipeline with three specialized agents (Ingestion, QA Director, Script Synthesizer). Supports dual-path execution (linear or graph via `LANGGRAPH_ENABLED`). See `docs/specs/FEATURE_SPEC_phase1_multi_agent.md`.
- **Document input mode**: Parse PDF and Markdown specs via pluggable OCR backends (`src/ocr_backends.py`). Extracts change deltas from headings, routes by persona role (QA lead, developer, product owner, operations), generates impact maps and consolidated reports.
- **OCR backend adapter**: `src/ocr_backends.py` with `PyMuPDFBackend` (CPU, default) and `UnlimitedOCRBackend` (GPU, 3B vision model). Auto-detects GPU/ROCm availability, configurable via `OCR_BACKEND` env var.
- **Journey URL inference**: When the journey scraper can't find a click target, probes common URL patterns (cart, checkout, finish) via HEAD requests and navigates there. SauceDemo checkout pages now reachable (5 pages scraped, was 2).
- **Mock server for eval**: `scripts/mock_server.py` uses `ThreadingHTTPServer` with daemon threads and error suppression. Auto-starts in eval runner for lv_insurance (eval-005).
- **AI-034 Test Table**: `src/test_table.py` — `TestRow`/`TestTable` data model with CRUD (add/remove/update/confirm), `TestTableExpander` (LLM expansion of conditions into concrete test rows; 1-row-per-condition fallback on LLM failure; cap 10 rows/condition), `table_to_conditions()` (confirmed rows → generation conditions). Test Table editors in both UIs: Streamlit `🧪 Test Table` expander (data editor + confirm-all) and CLI "Expand into Test Rows" menu flow. Living Test Plan gains a "Tests" column showing rows per condition. One skeleton function per confirmed row (`reviewed_conditions` in Streamlit, `_select_conditions_for_generation` in CLI). UAT: 2 conditions → 8 rows → 8 skeleton functions (1:1). `scripts/uat/uat_test_table.py`.

### Changed
- **AI-037 scoring/scraper behaviour**: synthetic ARIA-only containers no longer win Pass 1 fast-text or the B-025 container bonus for CLICK; text-content bonus now scales with overlap and normalises punctuation (`excess:` ≡ `excess`).
- **Prompt assembly migrated to t-strings**: `TestGenerator._generate_skeleton_single_call` and `Orchestrator._generate_single_condition_fragment` render prompts via `PromptBuilder` (byte-identical output to legacy `.format()`; verified by UAT). Both paths log structured `llm_call=... fields={...}` audit entries. Single-condition prompt now renders `{CLICK:...}` single-brace placeholders consistently with the main skeleton prompt (was literal `{{CLICK:...}}`).
- **Deterministic skeleton generation**: Planner and Generator agents now use `temperature=0` for reproducible graph pipeline output. Skeleton self-consistency: 55.6% → 100% (byte-for-byte identical across runs).
- **LLM provider temperature support**: `LLMProvider.complete()` and `LLMClient.generate()` now accept optional `temperature` parameter. Backward compatible — defaults to None (provider default).
- **PipelineGraph entry routing**: `PipelineState` now supports `input_mode`, `document_source`, `persona_role` fields. Document mode routes through `_parse_document` node before `ingest`.
- **AGENTS.md protected files**: Added `src/agents/` to protected files list.
- **Pre-commit mypy version**: `.pre-commit-config.yaml` mypy `v1.15.0` → `v2.3.0` (was crashing with INTERNAL ERROR on `src/agents/generator.py`).

### Fixed
- **B-028 fixed — journey discovery selects the cart nav link for product/add-to-cart actions** (see BACKLOG.md for full detail). Root causes: discovery passed lowercase actions to the scorer (all action bonuses silently disabled — "View Cart" won generic descriptions at score=1); hidden modals always present in e-commerce DOM crushed every real candidate with the modal penalty; POMs emitted hallucinated `text=<description>` locators; quantity inputs (role=number) were considered non-fillable; `tag` was missing from scraped elements (killing ASSERT display scoring). Fixes: action normalisation, visibility-aware modal penalty, product/category/dismiss context hints, DOM-existence index in generated POMs (`_ELEMENTS`), hidden-element exclusion from POM methods, `_is_fillable` aligned with `IntentMatcher`, `tag` added to `_build_element_dict`, FILL-quantity → +/- stepper fallback. Journey now visits product pages and reaches a non-empty cart (verify_production execution completes in ~65-75s; was 600s timeout; 12/13 gates). Full eval live-regenerate: 53.7% → 65.7% resolution accuracy; static 100% unchanged.
- **Batch placeholder fallback searches ALL scraped pages**: it was scoped to the seed URL, leaving elements on other scraped pages (e.g. `Proceed To Checkout` on view_cart) permanently unresolved. Per-journey resolution stays scoped for precision.
- **EvidenceTracker click fast-fail + proactive dismissal**: clicks on missing/hidden locators now fail in 0.0s with a clear error (was a 148s fallback marathon per click that blew the whole suite's timeout); hidden elements (CSRF inputs) and overlay-covered clicks are handled before the 5s wait. Consent/ad/confirmation-modal dismissal now runs proactively before each click (~2s vs 8-30s per blocked click).
- **Module-level LLM statement leaks stripped**: skeletons sometimes leaked bare calls (`home_page.click(...)`) outside test functions, crashing pytest at COLLECTION time. `normalise_generated_code` now strips module-level executable statements.
- **Structural assembler** (`src/test_structure_assembler.py`): the generated test file is rebuilt from the parsed journey model — the pipeline owns imports/decorators/def shells (built with a PEP 750 t-string); module-level leaks are structurally impossible. Wired into `run_pipeline` as the final pass.
- **LLM runaway generation capped**: providers now send `max_tokens`/`num_predict` (default 4096, `LLM_MAX_TOKENS`) — a runaway skeleton call no longer burns the full 600s request timeout.
- **Per-test pytest timeout**: UI/UAT/verify run commands now pass `--timeout=120` so a stuck test fails instead of hanging the suite.
- **LangGraph step count reduced 2.5x**: ScriptSynthesizerAgent now generates one skeleton fragment per condition (like linear pipeline) instead of all at once. Prevents cumulative prerequisite chaining where test N repeats all previous N-1 steps. LV Insurance: 137 → 52 placeholders, matching linear's 53.
- **B-027 re-fixed (properly)**: multi-concern unstructured requirements collapsed into a single condition. The 2026-07-24 comma-splitter fix had been reverted as too aggressive; the real fix adds SPLITTING RULES to the SpecAnalyzer prompt, routes wrapped single-item criteria ("1. <story>") to the LLM path, retries once with a CORRECTION on JSON parse failure, gates partial salvage (never silently drop corrupted objects), and falls back to sentence-boundary splitting (never mid-sentence commas). Verified: 1 story → journey + 2 boundary conditions. 10 new tests.
- **SpecAnalyzer JSON robustness**: LLM quoting story text verbatim inside `source` broke JSON parsing — prompt forbids embedded quotes, retry-once on parse failure, salvage gate.
- **Run Generated Tests UI**: `PIPELINE_TEST_TIMEOUT` default 300s → 600s (live-site suites exceeded 5 min). Run failures now render inline in the Run Generated Tests section (`run_tests_error`) instead of the off-screen `pipeline_error` slot near "Run Intelligent Pipeline".
- **LangGraph docs corrected**: pipeline is dormant (not wired into user flow — linear `run_pipeline` is the production path); `langgraph` is a core dependency and its tests run locally and in CI (71/71 pass).
- **LV Insurance SPA scraper fix**: Added `_reveal_hidden_sections()` to journey_scraper. Hidden SPA form sections (display:none) are now made visible before interaction via page.evaluate(). LV Insurance regeneration: 0% → 54%.
- **Eval captures at 100% resolution accuracy**: Fixed incorrect locators and assertions in pre-generated capture files (`scripts/eval/captures/`). AutomationExercise 50%→100%, DemoQA 88%→100%, TheInternet 86%→100%, SauceDemo 90%→100%. All 5 sites now at 100% static resolution accuracy.
- **LangGraph dependency handling**: Moved langgraph from optional `[langgraph]` extra to core dependency. `uv sync --upgrade` no longer silently drops it. Added `pytest.importorskip` guards to 3 test files for graceful degradation.
- **openai client pin**: Changed `openai==2.48.0` → `openai>=2.48.0` (was over-pinned during cherry-pick; it's just an HTTP client library). Updated to 2.50.0.

### Added
- **Cloud Provider Support**: `openai-compatible` and `openrouter` provider names for OpenRouter, Together AI, Groq, etc. via `OPENAI_COMPATIBLE_*` env vars. Uses existing `OpenAIProvider` with `is_openai_compatible` flag.
- **T-String Prompt Safety** (`src/agents/prompt_safety.py`): `safe_prompt()` wraps dynamic user input in `<user_input>` XML tags using Python 3.14 t-strings (PEP 750). Prevents prompt injection.
- **Graph Golden Keys** (`scripts/eval/dataset/graph/`): graph-specific evaluation keys extracted from captures via `scripts/eval/extract_graph_keys.py`.
- **POM Extraction in Validator**: `scripts/eval/golden_validator.py` now extracts POM calls (`inventory_page.click(...)`) alongside `evidence_tracker` calls.
- **Semantic Comparison Mode**: `python scripts/eval/eval_harness.py run --mode semantic` for locator-level comparison.
- **Phase 1 Multi-Agent Architecture (a-c)**: LangGraph-based `PipelineGraph` with three agents:
  - `IngestionAgent` — wraps `SpecAnalyzer` for criteria extraction + RAG domain enrichment
  - `QADirectorAgent` — priority assignment, prerequisite chaining, ambiguity flagging
  - `ScriptSynthesizerAgent` — delegates to `SkeletonGraph` for skeleton generation
  - Human-in-the-loop checkpoint: graph pauses after QA Director for test plan review
  - Enabled by default when langgraph installed (`pip install ai-playwright-generator[langgraph]`)
  - Graceful degradation to linear pipeline when langgraph not available
  - `LANGGRAPH_ENABLED=0` to force linear mode
  - 31 new tests, 1790 total, zero failures
- **AI-035 spec**: Self-Learning RAG — local pattern write-back from self-healing to RAG store
- **AI-030 Ingestion Agent**: `src/pdf_ingest.py` — PyMuPDF-based PDF extraction (heading detection, table extraction, chunking). `rag_ingest.py --pdfs` ingests domain PDFs into the RAG vector store. 3 real LV Insurance policy PDFs ingested → 66 chunks. RAG accuracy 53.7 → 64.2% (+10.5pp).
- **Phase 2b Self-Healing**: Rule-based pre-screening (`_pre_screen_failure()`) skips LLM call for assertion/navigation/other failures (cost optimization). Interactive repair fallback via `interactive_repair_candidates` in `HealingReport`.
- **Semantic scraper (B-032)**: Three-layer hybrid extraction — BS4 (structure) + CDP AX tree (accessible_name) + `page.aria_snapshot(boxes=True)` (placeholder, value, bbox, groups). Enabled by default; `SCRAPER_BACKEND=bs4` to disable.
- `src/aria_parser.py` — Parse Playwright's `aria_snapshot()` YAML output into standard element dicts (33 tests, all ARIA roles).
- `src/element_matcher.py` — Resolver accuracy improvements (B-024/B-025):
  - Pass1 word-ratio relax for short descriptions matching long element text
  - Pass1 heading skip for CLICK actions (headings are display elements, not click targets)
  - Pass1 id/name prefix match for FILL actions (e.g. "overnight" → `id="overnightLocation"`)
  - Pass1 word-boundary check for single-word containment (prevents "year" ⊆ "(years)" false positives)
- `src/placeholder_scorers.py` — Heading penalty (-20) for CLICK on elements without ID + container bonus (+10) for generic/div elements with ID (B-025)
- `scripts/eval/golden_validator.py` — Locator normalization: `#foo` ≡ `[id="foo"]`, `[data-test="bar"]` ≡ `.class[data-test="bar"]` (B-026)
- `docs/specs/FEATURE_SPEC_semantic_scraper.md` — Full design document for the semantic scraper transition
- `CONTEXT.md` — Updated architecture section with three-layer scraping + resolver pipeline
- `README.md` — Added "Semantic Scraper" feature bullet
- `docs/ARCHITECTURE.md` — Updated `PageScraper` description + added `aria_parser.py`

### Changed
- **Resolver accuracy improved**: 53.7% → 58.2% (+4.5pp, RAG off). LV Insurance: 83.3% → 95.8% (+12.5pp).
  - `_build_haystack`: added `id`, `accessible_name` + camelCase splitting
  - `_structural_bonus`: single-word ID match bonus (+15), `ref`→`reference` expansion, fixed camelCase ordering
  - `_split_camel_case()`: splits `quoteRef`→"quote Ref", `usageType`→"usage Type"
- **B-004, B-019 closed**: both fixed by architecture evolution (skeleton-first pipeline + semantic scraper)
- Self-healing: `HealingReport` gains `interactive_repair_candidates` field for Phase 2b fallback
- Roadmap hygiene: AI-028/AI-029/Phase 3 RAG/B-021 sub-checkboxes ticked, Phase 5 dataset expansion marked done, dual-tier eval marked `[R]` removed
- lv_insurance eval-005: **54.2% → 79.2%** (+25.0pp)
- Static eval harness: **79.1% → 88.1%** (+9.0pp vs baseline)
- `SCRAPER_BACKEND` env var now defaults to ARIA-hybrid; set to `bs4` for old behavior
- **Refactor 2026-07-11 — Journey scraper split:** `journey_scraper.py` (896→617 lines) split into 3 focused modules:
  - `src/journey_enrichment.py` — `capture_element_visibility_sync`, `capture_a11y_snapshot_sync` (deduplicated from `journey_executor.py`)
  - `src/cart_seeding_scraper.py` — `CartSeedingScraper` class (resolved circular import with `journey_scraper.py`)
  - `src/journey_subprocess.py` — `run_journey_subprocess_entry` subprocess entry point
- **Refactor 2026-07-11 — Placeholder orchestrator split:** `placeholder_orchestrator.py` (2,047→862 lines) split into 4 focused modules:
  - `src/role_mapper.py` — `DISPLAY_ROLES`, `_TAG_TO_ROLE`, `is_display_role`, `normalise_element_text`
  - `src/element_matcher.py` — Pass 0–3 matching engine, `ElementMatcher` class, B-020 semantic ASSERT resolution
  - `src/skip_manager.py` — consolidated skip insertion, placeholder line removal
  - `src/pom_helpers.py` — POM artifact generation, imports, instantiation, method calls

### Added
- CI/CD: parallelised quality gates — lint, type-check, sanitizer, and graph-freshness now run concurrently instead of sequentially (~15s saved per push)
- CI/CD: `graph-freshness` gate — warns when `graphify-out/graph.json` commit hash diverges from `HEAD`
- CI/CD: `docs-coverage` gate — warns when `markdown_docs/.sweep_progress.json` has pending files
- README.md: linked interactive call-flow diagram (`graphify-out/callflow.html`)
- README.md: added "Self-Documenting" feature bullet
- CONTRIBUTING.md: added Security section linking to SECURITY.md
- `scripts/maintenance/project_sanitizer.py`: replaced dead `links.csv` orphan audit with knowledge graph freshness check (reads `graphify-out/graph.json` `built_at_commit` vs `git HEAD`)
- `normalize_whitespace()` in `src/code_normalizer.py` — converts tabs to spaces and normalizes line endings (\r\n → \n) before other normalization transforms, preventing SyntaxError when LLMs emit tab-indented code
- `tests/test_code_normalizer.py` — 9 unit tests for `normalize_whitespace`, pipeline integration, and `ensure_test_navigation`
- AI-027 Session 2 screenshot capture during scraping: `ScrapeResult`, in-memory screenshot bytes, and interactive element bounding boxes for later vision enrichment.
- AI-027 Session 3 vision enrichment service: element crop, vision LLM call path, structured response parsing, and scraper enrichment bridge.
- **Refactor 2026-05-10 (Parts 1-7)** — Modular extraction reducing `streamlit_app.py` from 918 → 362 lines (60% reduction) per REFACTOR_PLAN_2026-05-10.md
  - `src/ui_pipeline.py` — Pipeline execution helpers extracted from `streamlit_app.py` (business logic, no rendering)
  - `src/ui_renderers.py` — Streamlit rendering helpers extracted from `streamlit_app.py` (pure UI, no business logic)
  - `src/evidence_serializer.py` — Evidence JSON serialization extracted from `evidence_tracker.py`
  - `src/screenshot_capture.py` — Screenshot capture utilities extracted from `evidence_tracker.py`
  - `src/state_tracker.py` — DOM state tracking extracted from `journey_scraper.py`
  - `src/form_detector.py` — Form detection and selector constants extracted from `journey_scraper.py`
  - `src/semantic_matcher.py` — Token-based semantic similarity extracted from `placeholder_resolver.py`
  - `src/intent_matcher.py` — Intent-based element filtering extracted from `placeholder_resolver.py`
  - `src/code_normalizer.py` — Code normalization transforms extracted from `code_postprocessor.py`
  - `src/llm_reasoning_filter.py` — LLM reasoning text detection extracted from `code_postprocessor.py`
  - `src/url_inference.py` — URL transition inference extracted from `placeholder_orchestrator.py`
- `CONTRIBUTING.md` — contributor guide with dev setup and coding standards
- `SECURITY.md` — private vulnerability reporting policy
- `CHANGELOG.md` — this file
- `CODE_OF_CONDUCT.md` — Contributor Covenant v2.1
- GitHub issue templates for bug reports and feature requests
- `src/analyzer.py` — CLI analysis module (replaces `cli/story_analyzer.py`)
- `src/config.py` — `AnalysisMode` and `ReportFormat` enums for CLI
- `src/code_postprocessor.py` — code string transformation helpers (extracted from `orchestrator.py`)
- `src/url_utils.py` — pure URL manipulation helpers (extracted from `orchestrator.py`)
- `src/report_builder.py` — report data preparation (extracted from `report_utils.py`)
- `src/report_formatters.py` — standard report renderers (extracted from `report_utils.py`)
- `src/evidence_report.py` — annotated screenshot/heatmap/journey generators (extracted from `report_utils.py`)

### Changed
- `cli/story_analyzer.py` → `cli/analyzer.py` — renamed for clarity
- `src/orchestrator.py` — extracted URL helpers to `src/url_utils.py` and code postprocessors to `src/code_postprocessor.py`
- `src/report_utils.py` — replaced with backwards-compatible re-export shim; logic moved to `report_builder.py`, `report_formatters.py`, and `evidence_report.py`

### Removed
- `cli/story_analyzer.py` — replaced by `cli/analyzer.py` + `src/config.py`
- `src/page_context_scraper.py` — deleted (deprecated, caused selector hallucination)
- Deprecated test files: `tests/src/`, `tests/example_test.py`, `tests/uat_pipeline_test.py`

### Fixed
- **Tab indentation SyntaxError** — LLMs emitting tab-indented code now normalized to spaces via `normalize_whitespace()` before `ensure_test_navigation()` injects 4-space indented navigation lines, preventing "unindent does not match" SyntaxErrors
- Pass 1 text match added to `PlaceholderOrchestrator._find_best_element_for_current_page()` — resolves nav links by element text before scoring, eliminating Products link tie bug
- Pass 1 text match added to `JourneyScraper._find_selector_for_step()` — journey discovery now navigates to correct pages (e.g. /products not /brand_products/*)  
- `resolve_all()` diagnostic replaced with regex scan of final_code — eliminates 25+ LLM timeout calls post-pipeline (runtime: 1263s → 165s)
- `src/journey_scraper_clean.py` dead file deleted (0% coverage, not imported)
- Punctuation stripping added to Pass 1 description normalisation — handles LLM-generated tokens like `'Products' link` with embedded quotes
- UAT saucedemo: 5/6 tests passing against real site with browser automation
- mypy `import-untyped` for pandas via `pandas-stubs` dev dependency
- mypy `import-untyped` for plotly via per-module override in `pyproject.toml`
- pre-commit hook failures from variable shadowing in `generate_3d_map.py` via mypy override
- Fix skeleton prompt generation to inject the exact expected test count into the LLM prompt.
- Improve placeholder postprocessing to unwrap `evidence_tracker.xxx({{...}})` wrappers with optional whitespace.
- Placeholder resolution now collects candidates across ALL scraped pages before selecting the global best match, preventing low-quality matches from early pages when a much better match exists on a later page (e.g., finding a cart page element for "username input" instead of the login page element). Added `tests/test_global_best_resolution.py` with 5 regression tests.

---

## [0.3.0] — 2026-04-10

### Added
- Multi-provider LLM support (`src/llm_providers/`) — Ollama, OpenAI, Anthropic, OpenRouter
- Pipeline architecture (`src/orchestrator.py`, `src/pipeline_models.py`, `src/pipeline_writer.py`)
- Anchor link extraction in page context scraper
- `src/coverage_utils.py` — coverage display-mapping logic extracted from `streamlit_app.py`
- `src/run_utils.py` — test command construction with re-run-failed-only support
- `src/semantic_candidate_ranker.py` — context candidate prioritisation
- `src/placeholder_resolver.py` — resolves LLM-generated placeholders in test output
- `src/skeleton_parser.py` — handles skeleton test scripts
- Credential profile selection persistence in Streamlit session state

### Fixed
- Migration from `pip` to `uv` as the sole package manager
- Coverage map now correctly reflects run outcomes (B-008)
- Structured failure tracking (`failed_pages`) with backward compatibility

---

## [0.2.0] — 2026-03-29

### Added
- `src/user_story_parser.py` — parses Gherkin, Jira AC bullets, numbered, and free-form stories
- `src/code_validator.py` — `ast.parse()` validation guard before saving generated tests (B-009)
- Multi-page scraping Phase A — `scrape_multiple_pages()`, `MultiPageContext`, `ScraperState`
- `.env.example` updated with correct `OLLAMA_TIMEOUT=300` default

### Fixed
- Parser banner incorrect on mixed pass/fail runs (B-006) — added 2 regression tests
- Duplicate error panels in run results UI (B-007)
- `src/pytest_output_parser.py` missing from repo (BREAK-1)
- Session state wipe blanking run results panel (BREAK-2)

---

## [0.1.0] — 2026-03-13

### Added
- Streamlit UI (`streamlit_app.py`) as primary entry point
- `src/page_context_scraper.py` — subprocess-based Playwright DOM scraper
- `src/pytest_output_parser.py` — parses pytest stdout into structured `RunResult`
- `src/report_utils.py` — generates local, Jira, and standalone HTML evidence bundles
- `src/file_utils.py` — test file save, rename, and newline normalisation helpers
- `src/llm_client.py` — Ollama API client with configurable timeout
- `src/test_generator.py` — core test generation pipeline
- Three report download formats: `local.md`, `jira.md`, `standalone.html`
- Ollama model selector in sidebar (live fetch via `ollama list`)
- Auto-save generated tests to `generated_tests/`
- Coverage tab with number-based test-to-criterion matching
- Run Now flow with pytest subprocess execution
- `pytest.ini` with `testpaths = tests` (generated tests excluded from default run)
- `launch_ui.sh` and `launch_dev.sh` startup scripts

### Fixed
- LLM generates async tests instead of pytest sync format (B-001)
- LLM output occasionally has all imports on one line (B-002) — `normalise_code_newlines()`
- Generated tests not saved automatically (B-003)
- Mock server startup incorrectly bundled into general launch script (B-005)

---

[Unreleased]: https://github.com/lacattano/tancat-ai/tancat/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/lacattano/tancat-ai/tancat/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/lacattano/tancat-ai/tancat/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/lacattano/tancat-ai/tancat/releases/tag/v0.1.0

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
MANIFEST
.Python
.installed.cfg

# ── Build & Distribution ──────────────────────────────────────────────────────
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/

# ── Virtual Environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Environment & Secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
!.vscode/launch.json
.idea/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Docker ────────────────────────────────────────────────────────────────────
.docker/

# ── Logs & Debug Output ───────────────────────────────────────────────────────
*.log
logs/
streamlit.err
pytest_out*.txt
uat_output*.txt
vulture_report.txt

# ── Testing & Coverage ────────────────────────────────────────────────────────
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/
test-results/
exported_tests/
bench_*
# ── Type Checking ─────────────────────────────────────────────────────────────
.mypy_cache/

# ── Screenshots ───────────────────────────────────────────────────────────────
screenshots/*
!screenshots/.gitkeep
!assets/*.png

# ── Evidence Sidecars ────────────────────────────────────────────────────────
evidence/
# Root-level runtime artifact (empty DB created by local runs) — not tracked.
run_results.sqlite
# The export-gate golden fixture mirrors a real generated package, whose
# run-history DB lives in evidence/ — keep that one file tracked (B-032).
!fixtures/golden_package/evidence/
!fixtures/golden_package/evidence/run_results.sqlite
# ── Generated Test Files ──────────────────────────────────────────────────────
# Ignore all generated test outputs (test packages, reports, parsed cases)
generated_tests/test_*/
generated_tests/verify_*/
# Flat generated test *scripts* (verify_production writes test_<site>.py, not a package)
# NOTE: committing these is forbidden per AGENTS.md — this rule enforces that.
generated_tests/test_*.py
# resolve-and-learn output (large — screenshots + resolved tests)
generated_tests/resolved/
generated_tests/test_cases_*.*
generated_tests/test_report_*.*
generated_tests/pages/
generated_tests/uat_*/

# But specifically DO NOT ignore these files (needed for the tool to work)
!generated_tests/conftest.py
!generated_tests/mock_insurance_site.html

# ── MCP Memory Bank ────────────────
cline-mcp-memory-bank/
# ── memory-bank/ ────────────────
memory-bank/

package-lock.json

# ── Cline/AI agent scratch files ─────────────────────────────────────────────
tmp_*.py
FIX_SUMMARY.md
ISSUES_FOUND_AND_FIXES.md
*.png
!assets/*.png
!landing/*.png
!screenshots/.gitkeep
.tmp/

# ── Private / Commercial Strategy Docs ────────────────────────────────────────
docs/private/

# Playwright
node_modules/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
# ── Scratch Files ──────────────
# scratch/ removed 2026-08-21 (audit): benchmark material moved to ../llm-benchmarks,
# stale debug scripts deleted. Keep the dir ignored so agent scratch never gets tracked again.
scratch/

# ── notebooks ──────────────

notebooks/_debug_output.py
notebooks/.ipynb_checkpoints/
graphify-out/
results_*.json
.pi/
evidence/rag_store.db
*.db
scripts/eval/scraped_pages/
scripts/eval/generated_tests/
.mcp.json
nul
.playwright-mcp/
raw/
generated_tests/debug_*
unsloth_compiled_cache/
ci-workspace/

# Phase 7 CI action scratch workspace (generated by the action / local self-test)
.ai-test-workspace/

# Phase 7 CI action scratch workspace (no leading dot — the action input default)
ai-test-workspace/

# Git worktrees (parallel agent worktrees). The branches live in this repo; the
# working directories do not belong in it.
.worktrees/

# local zvec-grep search index (dev-only, regenerable)
.zvec-grep/
